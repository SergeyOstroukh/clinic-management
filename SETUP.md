# 📋 Подробная инструкция по настройке

## 🎯 Сценарий использования в клинике

Представьте ситуацию:
- У вас есть несколько компьютеров в клинике
- Один компьютер будет **сервером** (на нем запускается backend)
- Остальные компьютеры открывают **браузер** и подключаются к серверу

## 📍 Шаг 1: Выберите компьютер-сервер

**Рекомендации:**
- Выберите компьютер, который обычно включен (например, компьютер администратора)
- Или выделенный компьютер/ноутбук, который всегда работает
- Убедитесь, что он подключен к той же сети Wi-Fi/кабелю, что и остальные

## 🔧 Шаг 2: Установка на сервере

1. **Установите Node.js:**
   - Скачайте с https://nodejs.org/ (версия LTS)
   - Установите, перезагрузите компьютер

2. **Скопируйте папку проекта** на этот компьютер

3. **Откройте командную строку** (cmd) в папке проекта

4. **Установите зависимости:**
   ```bash
   npm run install-all
   ```

5. **Запустите сервер:**
   ```bash
   npm run dev
   ```

   Вы увидите:
   ```
   🚀 Сервер запущен на http://localhost:3001
   ```

## 🌐 Шаг 3: Узнайте IP-адрес сервера

1. На том же компьютере откройте **командную строку** (cmd)
2. Введите: `ipconfig`
3. Найдите строку **"IPv4-адрес"**
   - Например: `192.168.1.100`
   - Это адрес вашего сервера в сети

## 🔥 Шаг 4: Настройка файрвола (важно!)

**Windows:**
1. Откройте "Панель управления" → "Брандмауэр Защитника Windows"
2. Нажмите "Дополнительные параметры"
3. Выберите "Правила для входящих подключений"
4. Нажмите "Создать правило"
5. Выберите "Порт" → Далее
6. Выберите "TCP" и укажите порты: `3000, 3001`
7. Выберите "Разрешить подключение"
8. Отметьте все профили (Домен, Частная, Публичная)
9. Назовите правило: "Clinic Server"
10. Готово!

**Альтернативный способ (быстрее):**
```bash
# Откройте PowerShell от имени администратора и выполните:
netsh advfirewall firewall add rule name="Clinic Server" dir=in action=allow protocol=TCP localport=3000,3001
```

## 💻 Шаг 5: Подключение с других компьютеров

1. **Убедитесь, что компьютеры в одной сети** (тот же Wi-Fi или кабель)

2. **На любом другом компьютере:**
   - Откройте браузер (Chrome, Edge, Firefox)
   - Введите адрес: `http://192.168.1.100:3000`
     (замените на ваш IP-адрес из шага 3)
   - Готово! Вы видите интерфейс системы

3. **Создайте закладку** в браузере для удобства

## 🔄 Шаг 6: Автозапуск сервера (опционально)

Чтобы сервер запускался автоматически при включении компьютера:

### Вариант A: Планировщик задач Windows

1. Откройте "Планировщик заданий"
2. Создайте задачу:
   - Триггер: "При входе в систему"
   - Действие: "Запустить программу"
   - Программа: `cmd.exe`
   - Аргументы: `/c cd /d "D:\clinic" && npm run dev`
   - (Замените путь на ваш)

### Вариант B: PM2 (рекомендуется)

1. Установите PM2:
   ```bash
   npm install -g pm2
   ```

2. Создайте файл `ecosystem.config.js` в корне проекта:
   ```javascript
   module.exports = {
     apps: [{
       name: 'clinic-server',
       script: './server/index.js',
       cwd: 'D:/clinic', // замените на ваш путь
       instances: 1,
       autorestart: true,
       watch: false,
       max_memory_restart: '1G',
       env: {
         NODE_ENV: 'production',
         PORT: 3001
       }
     }]
   };
   ```

3. Запустите:
   ```bash
   pm2 start ecosystem.config.js
   pm2 save
   pm2 startup
   ```

## 🎨 Создание ярлыка на рабочем столе

Для удобства создайте ярлык на всех компьютерах:

1. Правой кнопкой на рабочем столе → "Создать" → "Ярлык"
2. Укажите адрес: `http://192.168.1.100:3000`
3. Назовите: "Клиника - Система управления"
4. Готово!

## ⚠️ Важные моменты

1. **Сервер должен быть запущен** - если выключить компьютер-сервер, другие не смогут подключиться
2. **Одна база данных** - все работают с одной базой, изменения видны всем сразу
3. **IP-адрес может измениться** - если IP меняется, используйте статический IP или настройте DHCP резервирование в роутере
4. **Безопасность** - для продакшена добавьте авторизацию пользователей

## 🆘 Решение проблем

**Не могу подключиться с другого компьютера:**
- Проверьте, что сервер запущен
- Проверьте IP-адрес (`ipconfig`)
- Проверьте файрвол
- Убедитесь, что компьютеры в одной сети

**Порт занят:**
- Измените порт в `server/index.js` (строка с `PORT`)
- Или остановите программу, которая использует порт

**База данных не создается:**
- Проверьте права доступа к папке `server/`
- Убедитесь, что Node.js установлен правильно

## 📞 Нужна помощь?

Если что-то не работает:
1. Проверьте, что Node.js установлен: `node --version`
2. Проверьте, что сервер запущен (должны быть сообщения в консоли)
3. Попробуйте открыть `http://localhost:3000` на самом сервере
4. Проверьте файрвол

