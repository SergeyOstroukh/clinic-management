# Настройка VPS на Hoster.by - Пошаговая инструкция

## 🎯 ШАГ 1: Закажите VPS

1. На странице калькулятора настройте:
   - **RAM:** 2 ГБ (рекомендую) или 1 ГБ (минимум)
   - **vCPU:** 1 шт.
   - **Диск:** 10 ГБ SSD (достаточно)
   - **IP:** 1 шт.

2. Нажмите "Добавить в корзину"

3. При оформлении заказа:
   - **Операционная система:** Выберите **Ubuntu 22.04 LTS**
   - **Период:** На ваше усмотрение (1 месяц для теста)

4. Оплатите заказ

5. **Дождитесь активации** (обычно 5-30 минут)

---

## 📧 ШАГ 2: Получите данные доступа

На email придет письмо с:
- **IP-адрес:** например `185.123.45.67`
- **Логин:** обычно `root`
- **Пароль:** например `Xy7kL9mP2qR5`

**СОХРАНИТЕ ЭТИ ДАННЫЕ!**

---

## 💻 ШАГ 3: Подключитесь к VPS

### На вашем Windows компьютере:

1. Откройте **PowerShell** (не обычная командная строка!)
   - Нажмите `Win + X`
   - Выберите "Windows PowerShell"

2. Введите команду (замените IP на свой):
   ```powershell
   ssh root@ваш-IP-адрес
   ```
   
   Например:
   ```powershell
   ssh root@185.123.45.67
   ```

3. При первом подключении появится вопрос:
   ```
   Are you sure you want to continue connecting (yes/no)?
   ```
   Напишите: `yes` и нажмите Enter

4. Введите пароль (при вводе символы не видны — это нормально)

5. Если все правильно — вы увидите:
   ```
   root@server:~#
   ```

✅ **Вы подключились к серверу!**

---

## 🛠️ ШАГ 4: Установите необходимое ПО

Копируйте и выполняйте команды **по одной**:

### 4.1. Обновите систему
```bash
apt update && apt upgrade -y
```
⏱️ Это займет 2-5 минут. Дождитесь завершения.

### 4.2. Установите Node.js 18
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
```
Затем:
```bash
apt-get install -y nodejs
```

Проверьте установку:
```bash
node --version
```
Должно показать: `v18.x.x`

### 4.3. Установите PM2 (менеджер процессов)
```bash
npm install -g pm2
```

Проверьте:
```bash
pm2 --version
```

### 4.4. Установите Git
```bash
apt install git -y
```

### 4.5. Установите Nginx (веб-сервер)
```bash
apt install nginx -y
```

### 4.6. Включите файрвол и откройте порты
```bash
ufw allow 22/tcp
ufw allow 80/tcp
ufw allow 443/tcp
ufw --force enable
```

---

## 📦 ШАГ 5: Загрузите проект на VPS

### Способ A: Через Git (если проект на GitHub)

Если вы уже загрузили проект на GitHub:
```bash
cd /opt
git clone https://github.com/ваш-username/clinic.git
cd clinic
```

### Способ B: Через SCP (прямо с вашего компьютера)

На вашем **Windows компьютере** откройте **новое окно PowerShell** (не закрывая SSH):

```powershell
scp -r D:\clinic root@ваш-IP:/opt/clinic
```

Введите пароль. Загрузка займет несколько минут.

**После загрузки вернитесь в SSH окно** и выполните:
```bash
cd /opt/clinic
```

---

## 📥 ШАГ 6: Установите зависимости

```bash
npm run install-all
```

⏱️ Это займет 3-5 минут. Дождитесь завершения.

---

## 🔨 ШАГ 7: Соберите React приложение

```bash
cd /opt/clinic/client
npm run build
cd /opt/clinic
```

⏱️ Это займет 1-2 минуты.

---

## ⚙️ ШАГ 8: Настройте переменные окружения

```bash
cd /opt/clinic/server
nano .env
```

В открывшемся редакторе напишите:
```
NODE_ENV=production
PORT=3001
```

**Сохраните файл:**
- Нажмите `Ctrl + O` (сохранить)
- Нажмите `Enter` (подтвердить имя файла)
- Нажмите `Ctrl + X` (выйти)

---

## 🚀 ШАГ 9: Запустите приложение

```bash
cd /opt/clinic/server
pm2 start index.js --name clinic
pm2 save
pm2 startup
```

Последняя команда выдаст команду, скопируйте и выполните её.

Проверьте статус:
```bash
pm2 status
```

Должно показать:
```
┌─────┬──────────┬─────────┬─────────┐
│ id  │ name     │ status  │ restart │
├─────┼──────────┼─────────┼─────────┤
│ 0   │ clinic   │ online  │ 0       │
└─────┴──────────┴─────────┴─────────┘
```

✅ **Приложение запущено!**

---

## 🌐 ШАГ 10: Настройте Nginx

### 10.1. Создайте конфигурацию
```bash
nano /etc/nginx/sites-available/clinic
```

Вставьте (замените `dantist-clinic.by` на ваш домен):
```nginx
server {
    listen 80;
    server_name dantist-clinic.by www.dantist-clinic.by;

    location / {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

**Сохраните:** `Ctrl+O`, `Enter`, `Ctrl+X`

### 10.2. Активируйте конфигурацию
```bash
ln -s /etc/nginx/sites-available/clinic /etc/nginx/sites-enabled/
```

### 10.3. Проверьте конфигурацию
```bash
nginx -t
```

Должно показать: `syntax is ok` и `test is successful`

### 10.4. Перезапустите Nginx
```bash
systemctl restart nginx
```

---

## 🔗 ШАГ 11: Настройте DNS домена

1. Зайдите в **панель управления Hoster.by**
2. Найдите раздел **"Домены"** или **"DNS"**
3. Выберите домен `dantist-clinic.by`
4. Найдите **DNS записи** или **"Управление DNS"**
5. Измените **A-запись:**
   - **Имя:** `@` (или пустое)
   - **Тип:** `A`
   - **Значение:** IP-адрес вашего VPS (например, `185.123.45.67`)
   - **TTL:** 3600 (или оставьте по умолчанию)

6. Если есть запись `www`:
   - **Имя:** `www`
   - **Тип:** `A`
   - **Значение:** тот же IP-адрес VPS

7. Сохраните изменения

⏱️ **Подождите 5-30 минут** пока DNS обновится

---

## ✅ ШАГ 12: Проверьте работу

### 12.1. Проверка по IP
Откройте в браузере:
```
http://ваш-IP-адрес
```

Должно открыться приложение!

### 12.2. Проверка по домену (после обновления DNS)
Откройте в браузере:
```
http://dantist-clinic.by
```

✅ **Все работает!**

---

## 🔒 ШАГ 13: Установите SSL (HTTPS)

После того как домен заработает:

```bash
apt install certbot python3-certbot-nginx -y
certbot --nginx -d dantist-clinic.by -d www.dantist-clinic.by
```

Следуйте инструкциям:
1. Введите email
2. Согласитесь с условиями (y)
3. Согласитесь на рассылку (y или n)

Certbot автоматически настроит HTTPS!

Теперь откройте:
```
https://dantist-clinic.by
```

🎉 **Все готово!**

---

## 📋 Полезные команды

### Посмотреть статус приложения
```bash
pm2 status
```

### Посмотреть логи
```bash
pm2 logs clinic
```

### Перезапустить приложение
```bash
pm2 restart clinic
```

### Остановить приложение
```bash
pm2 stop clinic
```

### Проверить статус Nginx
```bash
systemctl status nginx
```

### Перезапустить Nginx
```bash
systemctl restart nginx
```

---

## 🆘 Если что-то не работает

### Проверьте логи приложения
```bash
pm2 logs clinic
```

### Проверьте логи Nginx
```bash
tail -f /var/log/nginx/error.log
```

### Проверьте файрвол
```bash
ufw status
```

### Проверьте, слушает ли приложение порт
```bash
netstat -tlnp | grep 3001
```

---

## 📞 Когда обращаться за помощью

Если на каком-то шаге возникла ошибка:
1. Скопируйте текст ошибки
2. Скажите, на каком шаге застряли
3. Я помогу разобраться

---

## 🎯 Краткая схема

```
1. Купили VPS → Получили IP, логин, пароль
2. Подключились по SSH
3. Установили Node.js, PM2, Nginx
4. Загрузили проект
5. Установили зависимости
6. Собрали React
7. Запустили через PM2
8. Настроили Nginx
9. Направили домен на IP
10. Установили SSL
11. Готово!
```

---

Удачи! После покупки VPS пишите — пройдем все шаги вместе! 🚀

