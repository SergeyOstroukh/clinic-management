# 🚀 Следующие шаги после генерации SSH ключа

Вы уже на сервере Hoster.by и сгенерировали SSH ключ. Отлично! Теперь продолжаем установку.

## 📋 Что делать дальше:

### 1. Посмотрите ваш публичный ключ (если нужно для доступа с других компьютеров):

```bash
cat ~/.ssh/id_rsa.pub
```

Скопируйте вывод - это ваш публичный ключ. Его можно добавить в панели Hoster.by для доступа без пароля.

**Но сейчас это не обязательно!** Вы уже на сервере, можно продолжать установку.

---

## ⚙️ Шаг 1: Обновление системы

```bash
sudo apt update && sudo apt upgrade -y
```

---

## 📦 Шаг 2: Установка Node.js

```bash
# Установка Node.js 18.x (LTS версия)
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# Проверьте установку
node --version
npm --version
```

Должно показать:
- `v18.x.x` для Node.js
- `9.x.x` для npm

---

## 🔧 Шаг 3: Установка PM2 (менеджер процессов)

PM2 будет автоматически запускать приложение при перезагрузке сервера.

```bash
sudo npm install -g pm2
```

---

## 📥 Шаг 4: Загрузка проекта на сервер

### Вариант A: Через Git (если код на GitHub)

```bash
# Установите Git (если еще не установлен)
sudo apt install git -y

# Перейдите в папку /opt
cd /opt

# Клонируйте репозиторий (замените на ваш URL)
git clone https://github.com/ваш-username/clinic-management.git clinic

cd clinic
```

### Вариант B: Через SCP (с вашего компьютера)

**На вашем компьютере (Windows PowerShell):**

```powershell
# Замените на ваш IP и путь
scp -r D:\clinic dantistcil@ваш-ip:/opt/clinic
```

**На вашем компьютере (Mac/Linux):**

```bash
scp -r ./clinic dantistcil@ваш-ip:/opt/clinic
```

**Затем на сервере:**

```bash
cd /opt/clinic
```

### Вариант C: Создать проект вручную

Если хотите создать файлы прямо на сервере:

```bash
cd /opt
mkdir clinic
cd clinic
# Создайте структуру папок и файлы
```

---

## 📦 Шаг 5: Установка зависимостей

```bash
cd /opt/clinic

# Установите все зависимости
npm run install-all

# Соберите React приложение
npm run build
```

Это займет несколько минут.

---

## ⚙️ Шаг 6: Настройка переменных окружения

```bash
cd /opt/clinic/server
nano .env
```

Добавьте в файл:
```
NODE_ENV=production
PORT=3001
```

**Сохраните:**
- Нажмите `Ctrl+O` (сохранить)
- Нажмите `Enter` (подтвердить)
- Нажмите `Ctrl+X` (выйти)

---

## 🚀 Шаг 7: Запуск приложения

```bash
cd /opt/clinic/server
pm2 start index.js --name clinic
pm2 save
pm2 startup
```

**Проверьте работу:**

```bash
pm2 status
pm2 logs clinic
```

Вы должны увидеть:
```
🚀 Сервер запущен на порту 3001
```

---

## 🌐 Шаг 8: Проверка доступа

Откройте в браузере:
```
http://ваш-ip-адрес:3001
```

Если не открывается, проверьте файрвол:

```bash
# Разрешить порт 3001
sudo ufw allow 3001/tcp
sudo ufw enable
```

---

## 📝 Полезные команды PM2:

```bash
pm2 status              # Статус приложения
pm2 logs clinic         # Просмотр логов (Ctrl+C для выхода)
pm2 restart clinic      # Перезапуск
pm2 stop clinic         # Остановка
pm2 delete clinic       # Удаление
pm2 monit               # Мониторинг в реальном времени
```

---

## 🆘 Если что-то пошло не так:

### Приложение не запускается:
```bash
pm2 logs clinic
```

### Проверьте права доступа:
```bash
ls -la /opt/clinic/server/
```

### Проверьте, что порт свободен:
```bash
sudo netstat -tulpn | grep 3001
```

---

## ✅ Готово!

После выполнения всех шагов ваше приложение будет доступно по адресу:
- `http://ваш-ip-адрес:3001`

Все пользователи смогут работать из любой точки мира! 🎉

