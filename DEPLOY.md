# 🚀 Развертывание в облаке для доступа из любой точки

Этот гайд поможет развернуть приложение на облачном хостинге, чтобы можно было работать из любой точки мира.

## 📋 Варианты размещения

### Вариант 1: Railway.app (БЕСПЛАТНО для начала) ⭐ Рекомендуется

**Плюсы:**
- ✅ Бесплатный тариф (500 часов/месяц)
- ✅ Автоматический деплой из GitHub
- ✅ Бесплатный домен
- ✅ Простая настройка
- ✅ Автоматические SSL сертификаты

**Шаги:**

1. **Создайте аккаунт на Railway:**
   - Перейдите на https://railway.app
   - Войдите через GitHub

2. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий

3. **Настройте переменные окружения:**
   - В настройках проекта → Variables
   - Добавьте:
     ```
     NODE_ENV=production
     PORT=3001
     ```

4. **Настройте деплой:**
   - Railway автоматически определит Node.js проект
   - В настройках Build Command: `npm run install-all && npm run build`
   - Start Command: `cd server && npm start`

5. **Получите URL:**
   - Railway даст вам URL типа: `https://your-app.railway.app`
   - Откройте его в браузере - готово!

---

### Вариант 2: Render.com (БЕСПЛАТНО)

**Плюсы:**
- ✅ Бесплатный тариф
- ✅ Автоматический деплой
- ✅ Бесплатный домен

**Шаги:**

1. **Создайте аккаунт на Render:**
   - https://render.com
   - Войдите через GitHub

2. **Создайте новый Web Service:**
   - New → Web Service
   - Подключите репозиторий

3. **Настройки:**
   - **Build Command:** `npm run install-all && npm run build`
   - **Start Command:** `cd server && npm start`
   - **Environment:** Node
   - **Environment Variables:**
     ```
     NODE_ENV=production
     PORT=3001
     ```

4. **Деплой:**
   - Нажмите "Create Web Service"
   - Render автоматически развернет приложение
   - Получите URL: `https://your-app.onrender.com`

---

### Вариант 3: Fly.io (БЕСПЛАТНО)

**Плюсы:**
- ✅ Бесплатный тариф
- ✅ Глобальная сеть
- ✅ Быстрая работа

**Шаги:**

1. **Установите Fly CLI:**
   ```bash
   # Windows (PowerShell)
   powershell -Command "iwr https://fly.io/install.ps1 -useb | iex"
   ```

2. **Войдите:**
   ```bash
   fly auth login
   ```

3. **Создайте приложение:**
   ```bash
   fly launch
   ```

4. **Настройте fly.toml** (создайте файл в корне проекта):
   ```toml
   app = "your-clinic-app"
   primary_region = "iad"

   [build]
     builder = "paketobuildpacks/builder:base"

   [http_service]
     internal_port = 3001
     force_https = true
     auto_stop_machines = true
     auto_start_machines = true
     min_machines_running = 0
     processes = ["app"]

   [[services]]
     http_checks = []
     internal_port = 3001
     processes = ["app"]
     protocol = "tcp"
     script_checks = []

     [services.concurrency]
       hard_limit = 25
       soft_limit = 20
       type = "connections"

     [[services.ports]]
       handlers = ["http"]
       port = 80

     [[services.ports]]
       handlers = ["tls", "http"]
       port = 443

   [env]
     NODE_ENV = "production"
     PORT = "3001"
   ```

5. **Деплой:**
   ```bash
   fly deploy
   ```

---

### Вариант 4: VPS сервер (платно, но надежно)

**Рекомендуемые провайдеры:**
- **Hoster.by** (Беларусь) - от ~15-20 BYN/месяц ⭐ Для Беларуси
- **Timeweb** (Россия) - от 300₽/месяц
- **Hetzner** (Германия) - от 4€/месяц
- **DigitalOcean** - от $6/месяц

---

### 🎯 Развертывание на Hoster.by VPS

**Hoster.by** - белорусский хостинг-провайдер, отлично подходит для размещения Node.js приложений.

**Что нужно:**
- VPS/VDS сервер (не виртуальный хостинг!)
- Минимум: 1 ГБ RAM, 1 CPU, 10 ГБ диска
- Рекомендуется: 2 ГБ RAM для комфортной работы

**Шаги развертывания:**

1. **Закажите VPS на Hoster.by:**
   - Зайдите на https://hoster.by
   - Выберите "Виртуальные серверы" (VPS/VDS)
   - Выберите тариф (рекомендуется от 2 ГБ RAM)
   - Выберите ОС: **Ubuntu 20.04** или **Ubuntu 22.04**
   - Оформите заказ

2. **Получите доступ:**
   - После активации вы получите:
     - IP-адрес сервера
     - Логин (обычно `root`)
     - Пароль (или SSH ключ)

3. **Подключитесь по SSH:**
   
   **Windows (PowerShell или PuTTY):**
   ```bash
   ssh root@ваш-ip-адрес
   ```
   
   Или используйте PuTTY:
   - Скачайте PuTTY: https://www.putty.org/
   - Введите IP-адрес
   - Port: 22
   - Connection type: SSH
   - Нажмите "Open"
   - Нажмите "Open"

4. **Обновите систему:**
   ```bash
   apt update && apt upgrade -y
   ```

5. **Установите Node.js:**
   ```bash
   # Установка Node.js 18.x (LTS)
   curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
   apt-get install -y nodejs
   
   # Проверьте установку
   node --version
   npm --version
   ```

6. **Установите PM2 (менеджер процессов):**
   ```bash
   npm install -g pm2
   ```

7. **Установите Git (если нужно):**
   ```bash
   apt install git -y
   ```


8. **Скопируйте проект на сервер:**
   
   **Вариант A: Через Git (рекомендуется)**
   ```bash
   cd /opt
   git clone https://github.com/ваш-username/clinic-management.git clinic
   cd clinic
   ```
   
   **Вариант B: Через SCP (с вашего компьютера)**
   ```bash
   # На вашем компьютере (Windows PowerShell)
   scp -r D:\clinic root@ваш-ip:/opt/clinic
   ```
   
   **Вариант C: Через файловый менеджер Hoster.by**
   - Используйте панель управления хостингом
   - Загрузите файлы через FTP/SFTP

9. **Установите зависимости:**
   ```bash
   cd /opt/clinic
   npm run install-all
   npm run build
   ```

10. **Создайте файл .env:**
    ```bash
    cd /opt/clinic/server
    nano .env
    ```
    
    Добавьте:
    ```
    NODE_ENV=production
    PORT=3001
    ```
    
    Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

11. **Запустите приложение с PM2:**
    ```bash
    cd /opt/clinic/server
    pm2 start index.js --name clinic
    pm2 save
    pm2 startup
    ```
    
    PM2 автоматически запустит приложение при перезагрузке сервера.

12. **Проверьте работу:**
    ```bash
    pm2 status
    pm2 logs clinic
    ```

13. **Настройте файрвол (если нужно):**
    ```bash
    # Разрешить порт 3001
    ufw allow 3001/tcp
    ufw enable
    ```

14. **Настройте домен (опционально):**
    
    Если у вас есть домен на Hoster.by:
    
    a. **Настройте DNS:**
       - В панели управления доменом добавьте A-запись:
         - Имя: `@` или `www`
         - Значение: IP-адрес вашего VPS
         - TTL: 3600
    
    b. **Установите Nginx:**
       ```bash
       apt install nginx -y
       ```
    
    c. **Создайте конфигурацию:**
       ```bash
       nano /etc/nginx/sites-available/clinic
       ```
       
       Добавьте:
       ```nginx
       server {
           listen 80;
           server_name ваш-домен.by;

           location / {
               proxy_pass http://localhost:3001;
               proxy_http_version 1.1;
               proxy_set_header Upgrade $http_upgrade;
               proxy_set_header Connection 'upgrade';
               proxy_set_header Host $host;
               proxy_set_header X-Real-IP $remote_addr;
               proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
               proxy_set_header X-Forwarded-Proto $scheme;
               proxy_cache_bypass $http_upgrade;
           }
       }
       ```
    
    d. **Активируйте конфигурацию:**
       ```bash
       ln -s /etc/nginx/sites-available/clinic /etc/nginx/sites-enabled/
       nginx -t
       systemctl restart nginx
       ```
    
    e. **Настройте SSL (Let's Encrypt):**
       ```bash
       apt install certbot python3-certbot-nginx -y
       certbot --nginx -d ваш-домен.by
       ```

15. **Готово!** 🎉
    
    - **По IP:** `http://ваш-ip:3001`
    - **По домену:** `https://ваш-домен.by`

**Полезные команды PM2:**
```bash
pm2 status              # Статус приложения
pm2 logs clinic         # Просмотр логов
pm2 restart clinic      # Перезапуск
pm2 stop clinic         # Остановка
pm2 delete clinic       # Удаление
pm2 monit               # Мониторинг в реальном времени
```

**Резервное копирование базы данных:**
```bash
# Создайте скрипт
nano /opt/clinic/backup.sh
```

Добавьте:
```bash
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/opt/clinic/backups"
mkdir -p $BACKUP_DIR
cp /opt/clinic/server/clinic.db $BACKUP_DIR/clinic_$DATE.db
# Удаляем старые бэкапы (старше 30 дней)
find $BACKUP_DIR -name "clinic_*.db" -mtime +30 -delete
```

Сделайте исполняемым:
```bash
chmod +x /opt/clinic/backup.sh
```

Добавьте в cron (каждый день в 2:00):
```bash
crontab -e
# Добавьте строку:
0 2 * * * /opt/clinic/backup.sh
```

---

### Вариант 4 (общий): VPS сервер других провайдеров

**Шаги:**

1. **Купите VPS сервер** (Ubuntu 20.04 или новее)

2. **Подключитесь по SSH:**
   ```bash
   ssh root@your-server-ip
   ```

3. **Установите Node.js:**
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   sudo apt-get install -y nodejs
   ```

4. **Установите PM2:**
   ```bash
   sudo npm install -g pm2
   ```

5. **Скопируйте проект на сервер:**
   ```bash
   # На вашем компьютере
   scp -r ./clinic root@your-server-ip:/opt/
   
   # Или используйте Git
   git clone your-repo-url /opt/clinic
   ```

6. **На сервере установите зависимости:**
   ```bash
   cd /opt/clinic
   npm run install-all
   npm run build
   ```

7. **Создайте файл .env:**
   ```bash
   cd /opt/clinic/server
   nano .env
   ```
   Добавьте:
   ```
   NODE_ENV=production
   PORT=3001
   ```

8. **Запустите с PM2:**
   ```bash
   cd /opt/clinic/server
   pm2 start index.js --name clinic
   pm2 save
   pm2 startup
   ```

9. **Настройте Nginx (опционально, для домена):**
   ```bash
   sudo apt install nginx
   sudo nano /etc/nginx/sites-available/clinic
   ```
   
   Добавьте:
   ```nginx
   server {
       listen 80;
       server_name your-domain.com;

       location / {
           proxy_pass http://localhost:3001;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
       }
   }
   ```
   
   Активируйте:
   ```bash
   sudo ln -s /etc/nginx/sites-available/clinic /etc/nginx/sites-enabled/
   sudo nginx -t
   sudo systemctl restart nginx
   ```

10. **Настройте SSL (Let's Encrypt):**
   ```bash
   sudo apt install certbot python3-certbot-nginx
   sudo certbot --nginx -d your-domain.com
   ```

---

## 🔧 Настройка для продакшена

### 1. Обновите API URL в клиенте

После развертывания обновите `client/src/App.js`:

```javascript
// Замените на ваш URL
const API_URL = process.env.REACT_APP_API_URL || 'https://your-app.railway.app/api';
```

Или используйте переменную окружения при сборке:
```bash
REACT_APP_API_URL=https://your-app.railway.app/api npm run build
```

### 2. Настройте CORS (если нужно)

В `server/index.js` уже настроен CORS, но можно ограничить доступ:

```javascript
// В переменных окружения
CORS_ORIGIN=https://your-domain.com
```

### 3. Резервное копирование базы данных

**Важно:** SQLite база данных хранится на сервере. Настройте автоматическое резервное копирование:

```bash
# Создайте скрипт backup.sh
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
cp /opt/clinic/server/clinic.db /opt/clinic/backups/clinic_$DATE.db

# Добавьте в crontab (каждый день в 2:00)
# 0 2 * * * /opt/clinic/backup.sh
```

---

## 📱 Использование после развертывания

1. **Откройте URL** в браузере (например, `https://your-app.railway.app`)
2. **Работайте из любой точки мира** - просто откройте браузер
3. **Все данные синхронизируются** - изменения видны всем пользователям сразу

---

## 🔒 Безопасность (рекомендуется добавить)

Для продакшена добавьте авторизацию:

1. **Установите библиотеку для авторизации:**
   ```bash
   cd server
   npm install jsonwebtoken bcryptjs
   ```

2. **Создайте систему логина** (можно добавить позже)

---

## 💡 Рекомендации

- **Для начала:** Используйте Railway или Render (бесплатно)
- **Для продакшена:** VPS с доменом и SSL
- **Резервное копирование:** Настройте автоматические бэкапы базы данных
- **Мониторинг:** Используйте PM2 для мониторинга на VPS

---

## 🆘 Решение проблем

**Приложение не запускается:**
- Проверьте логи: `pm2 logs` (на VPS) или логи в панели хостинга
- Убедитесь, что переменные окружения установлены
- Проверьте, что порт правильный

**База данных не работает:**
- Убедитесь, что у процесса есть права на запись в папку `server/`
- Проверьте путь к базе данных

**CORS ошибки:**
- Проверьте настройки CORS_ORIGIN
- Убедитесь, что URL правильный

