# Функционал отображения временных слотов в календаре врачей

## Описание

Добавлена функция отображения временных слотов в календаре расписания врачей с цветовой индикацией:
- 🟢 **Зеленый цвет** - свободный слот
- 🔴 **Красный цвет** - занятый слот (есть запись)

## Что было сделано

### 1. Серверная часть (Backend)

**Файл:** `server/index.js`

Добавлен новый API endpoint для получения записей врача на конкретный день:

```javascript
GET /api/doctors/:id/daily-appointments?date=YYYY-MM-DD
```

Этот endpoint возвращает все записи (appointments) для конкретного врача на указанную дату.

### 2. Клиентская часть (Frontend)

#### 2.1 Компонент TimeSlots

**Файлы:** 
- `client/src/components/DoctorSchedule/TimeSlots.js`
- `client/src/components/DoctorSchedule/TimeSlots.css`

Новый компонент для отображения временных слотов с функциями:

- **Генерация слотов**: Автоматически генерирует временные слоты с заданным интервалом (по умолчанию 30 минут)
- **Проверка занятости**: Проверяет для каждого слота наличие записи
- **Цветовая индикация**:
  - Зеленый градиент для свободных слотов
  - Красный градиент для занятых слотов
- **Детальная информация**: При клике на занятый слот отображается информация о клиенте (ФИО, телефон, примечания)
- **Статусы записей**: Отображаются иконки статуса записи (📅 запланирована, ✅ завершена, ❌ отменена, 🔄 в процессе)

#### 2.2 Интеграция в ScheduleCalendar

**Файлы:**
- `client/src/components/DoctorSchedule/ScheduleCalendar.js`
- `client/src/components/DoctorSchedule/ScheduleCalendar.css`

Изменения:

- Добавлена кнопка "👁" на днях с расписанием для просмотра слотов (только для не-админов)
- При клике на день (или кнопку) отображается секция с временными слотами
- Выбранный день подсвечивается синим цветом
- Добавлена легенда с подсказкой "👁 Нажмите на день для просмотра слотов"
- Кнопка "✖ Закрыть" для скрытия слотов

#### 2.3 Обновление DoctorSchedule

**Файл:** `client/src/components/DoctorSchedule/DoctorSchedule.js`

Добавлена передача `doctorId` в компонент `ScheduleCalendar` для корректной работы TimeSlots.

## Как использовать

1. **Для администраторов (superadmin)**:
   - Открыть "Расписание врачей"
   - Выбрать врача
   - Перейти в "⚙️ Управление расписанием"
   - Клик на день в календаре для редактирования расписания

2. **Для врачей и других ролей**:
   - Открыть "Расписание врачей"
   - Выбрать врача
   - Перейти в "⚙️ Управление расписанием"
   - Кликнуть на иконку "👁" на дне с расписанием ИЛИ просто кликнуть на день
   - Откроется секция с временными слотами:
     - 🟢 Зеленые - свободные слоты
     - 🔴 Красные - занятые слоты
   - Клик на занятый слот покажет детали записи

## Технические детали

### Генерация временных слотов

Функция `generateTimeSlots(startTime, endTime, intervalMinutes)` создает массив временных слотов:

```javascript
// Пример: с 09:00 до 17:00 с интервалом 30 минут
// Результат: ['09:00', '09:30', '10:00', '10:30', ... '16:30']
```

### Проверка занятости слота

Функция `isSlotOccupied(slotTime, appointments, intervalMinutes)` проверяет:
- Есть ли запись (appointment) с временем, попадающим в данный слот
- Учитывает интервал слота (например, слот 09:00 охватывает время с 09:00 до 09:30)

### Адаптивность

Все компоненты адаптированы для мобильных устройств:
- Изменение размеров слотов
- Перестройка сетки
- Адаптация кнопок и заголовков

## Примечания

- Интервал слотов по умолчанию: **30 минут** (можно изменить через prop `intervalMinutes`)
- Слоты генерируются на основе времени работы врача из расписания (specific dates или regular schedule)
- При наличии записи на конкретное время, соответствующий слот помечается как занятый
- Прошедшие дни в календаре отображаются полупрозрачными и неактивными

## Возможные улучшения

1. Добавить возможность создания записи прямо из свободного слота
2. Подсветка текущего времени (если день = сегодня)
3. Фильтрация слотов по статусу записи
4. Экспорт расписания на день в PDF
5. Настройка интервала слотов через UI

