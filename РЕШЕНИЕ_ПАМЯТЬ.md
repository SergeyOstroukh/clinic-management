# 🔧 Решение проблемы с памятью при сборке

## ❌ Проблема: "Out of memory: wasm memory"

На shared hosting не хватает памяти для сборки React приложения. Это нормально.

## ✅ РЕШЕНИЕ: Соберите приложение на вашем компьютере

Соберите приложение локально и загрузите готовую папку `build` на сервер.

---

## 📦 ШАГ 1: Соберите приложение на вашем Windows компьютере

### На вашем компьютере (PowerShell):

1. **Откройте PowerShell**
2. **Перейдите в папку проекта:**
   ```powershell
   cd D:\clinic
   ```

3. **Установите зависимости (если еще не установлены):**
   ```powershell
   npm run install-all
   ```

4. **Соберите приложение:**
   ```powershell
   cd client
   npm run build
   ```

   Подождите... Создастся папка `build` с готовыми файлами.

---

## 📤 ШАГ 2: Загрузите папку build на сервер

### Через WinSCP:

1. **Откройте WinSCP**
2. **Подключитесь к серверу** (SFTP, порт 22)
3. **Слева:** Найдите папку `D:\clinic\client\build`
4. **Справа:** Перейдите в `/home/dantistcil/dantist-cilinic.by/id_rsa/client/`
5. **Перетащите папку `build`** из левой панели в правую

---

## 🚀 ШАГ 3: Запустите сервер на хостинге

### В терминале на сервере:

```bash
cd ~/dantist-cilinic.by/id_rsa/server
```

Создайте файл .env:

```bash
nano .env
```

Добавьте:
```
NODE_ENV=production
PORT=3001
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

Установите PM2:

```bash
npm install -g pm2
```

Запустите:

```bash
pm2 start index.js --name clinic
pm2 save
```

---

## ✅ Альтернатива: Увеличьте лимит памяти (если хотите собрать на сервере)

Попробуйте собрать с увеличенным лимитом памяти:

```bash
cd ~/dantist-cilinic.by/id_rsa/client
NODE_OPTIONS="--max-old-space-size=4096" npm run build
```

Но на shared hosting это может не сработать из-за ограничений.

---

## 💡 Рекомендация

**Соберите локально** - это быстрее и надежнее!

1. Соберите на своем компьютере
2. Загрузите папку `build` на сервер
3. Запустите сервер

**Готово!** 🎉

