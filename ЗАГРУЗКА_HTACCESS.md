# 📤 Загрузка файла .htaccess на хостинг

## ✅ Файл создан!

Файл `.htaccess` создан в папке `public_html/.htaccess` в вашем проекте.

---

## 📤 ШАГ 1: Загрузите файл на хостинг

### Способ A: Через WinSCP (РЕКОМЕНДУЕТСЯ)

1. **Откройте WinSCP**
2. **Подключитесь к серверу:**
   - Протокол: `SFTP`
   - Хост: `93.125.99.149`
   - Порт: `22`
   - Логин: `dantistcil`
   - Пароль: ваш пароль

3. **Слева (ваш компьютер):**
   - Найдите файл: `D:\clinic\public_html\.htaccess`

4. **Справа (сервер):**
   - Перейдите в папку: `/home/dantistcil/public_html`

5. **Перетащите файл** `.htaccess` из левой панели в правую

6. **Готово!** Файл загружен!

---

### Способ B: Через файловый менеджер в панели

1. **В панели управления** откройте **файловый менеджер**
2. **Перейдите в папку** `/home/dantistcil/public_html`
3. **Нажмите "Загрузить файл"** или **"Upload"**
4. **Выберите файл** `D:\clinic\public_html\.htaccess`
5. **Загрузите**

---

### Способ C: Через SCP (командная строка)

На вашем компьютере (PowerShell):

```powershell
scp D:\clinic\public_html\.htaccess dantistcil@93.125.99.149:/home/dantistcil/public_html/.htaccess
```

Введите пароль когда попросит.

---

## ✅ ШАГ 2: Проверьте, что файл загружен

В терминале на сервере:

```bash
ls -la /home/dantistcil/public_html/.htaccess
```

Должен показать файл `.htaccess`.

Или проверьте содержимое:

```bash
cat /home/dantistcil/public_html/.htaccess
```

Должен показать содержимое файла.

---

## ✅ ШАГ 3: Проверьте работу

1. **Убедитесь, что файлы React приложения в `/public_html`:**
   ```bash
   ls -la /home/dantistcil/public_html/
   ```
   
   Должны быть:
   - `index.html`
   - Папка `static`
   - Файл `.htaccess`

2. **Убедитесь, что сервер запущен:**
   ```bash
   pm2 status
   ```

3. **Откройте в браузере:**
   ```
   http://dantist-cilinic.by
   ```

   Должно открыться ваше приложение! 🎉

---

## ⚠️ Важно: Имя файла

Файл должен называться **`.htaccess`** (с точкой в начале!)

- ✅ Правильно: `.htaccess`
- ❌ Неправильно: `htaccess` или `.htaccess.txt`

Если при загрузке точка пропадает:
- Переименуйте на сервере: `mv htaccess .htaccess`

---

## 🆘 Если не работает

### Проблема 1: Файл не виден

Файлы, начинающиеся с точки, могут быть скрыты. В файловом менеджере включите "Показать скрытые файлы".

### Проблема 2: Ошибка 500

Проверьте синтаксис `.htaccess`. Убедитесь, что:
- Все строки правильные
- Нет лишних пробелов
- Файл сохранен в правильной кодировке (UTF-8)

### Проблема 3: Не работает проксирование

Убедитесь, что:
- Модуль `mod_rewrite` включен (обычно включен по умолчанию)
- Модуль `mod_proxy` включен (для проксирования)

Если не работает, попробуйте упрощенный вариант `.htaccess`:

```apache
RewriteEngine On
RewriteCond %{REQUEST_URI} ^/api
RewriteRule ^(.*)$ http://localhost:3001/api/$1 [P,L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
```

---

## 📋 Чек-лист

- [ ] Файл `.htaccess` создан локально
- [ ] Файл загружен в `/home/dantistcil/public_html/`
- [ ] Файл называется `.htaccess` (с точкой!)
- [ ] Файлы React приложения в `/public_html`
- [ ] Сервер запущен (`pm2 status`)
- [ ] Открывается `http://dantist-cilinic.by`

**После выполнения всех пунктов - все должно работать!** 🎉

