# 🔧 Решение: Домен указывает не туда

## ❌ Проблема:

- **Домен** `dantist-cilinic.by` → указывает на `/public_html`
- **Проект** находится в `/home/dantistcil/dantist-cilinic.by/id_rsa/`
- **Приложение** работает на порту `3001`

**Результат:** Домен открывает пустую папку `/public_html`, а не ваше приложение!

---

## ✅ РЕШЕНИЕ 1: Создать редирект в public_html (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Создайте файл в public_html

В файловом менеджере или через терминал:

1. **Перейдите в папку** `/home/dantistcil/public_html`
2. **Создайте файл** `.htaccess` (если Apache) или `index.html`

### Шаг 2: Настройте редирект

**Вариант A: Через .htaccess (для Apache)**

Создайте файл `.htaccess` в `/public_html`:

```apache
RewriteEngine On
RewriteCond %{REQUEST_URI} !^/api
RewriteRule ^(.*)$ http://localhost:3001/$1 [P,L]
ProxyPassReverse / http://localhost:3001/
```

**Вариант B: Через index.html (проще)**

Создайте файл `index.html` в `/public_html`:

```html
<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="refresh" content="0; url=http://localhost:3001">
    <script>
        window.location.href = "http://localhost:3001";
    </script>
</head>
<body>
    <p>Перенаправление на приложение...</p>
</body>
</html>
```

---

## ✅ РЕШЕНИЕ 2: Переместить собранное приложение в public_html

### Шаг 1: Скопируйте папку build

В терминале:

```bash
# Скопируйте собранное приложение
cp -r ~/dantist-cilinic.by/id_rsa/client/build /home/dantistcil/public_html/
```

### Шаг 2: Создайте файл для API

Создайте файл `.htaccess` в `/public_html`:

```apache
RewriteEngine On

# API запросы проксируем на сервер
RewriteCond %{REQUEST_URI} ^/api
RewriteRule ^(.*)$ http://localhost:3001/$1 [P,L]

# Остальные запросы - на React приложение
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
```

### Шаг 3: Запустите сервер отдельно

Сервер должен работать на порту 3001 (уже запущен через PM2).

---

## ✅ РЕШЕНИЕ 3: Изменить корневую папку домена

### Через панель управления:

1. **Нажмите "Управлять"** рядом с доменом `dantist-cilinic.by`
2. **Найдите настройку "Корневая папка"** или **"Document Root"**
3. **Измените на:** `/home/dantistcil/dantist-cilinic.by/id_rsa/client/build`
4. **Сохраните**

**⚠️ Внимание:** На shared hosting это может быть недоступно. Если нет такой опции - используйте Решение 1 или 2.

---

## ✅ РЕШЕНИЕ 4: Использовать поддомен (если основной домен нельзя изменить)

1. **Создайте поддомен:** `app.dantist-cilinic.by`
2. **Настройте его** на папку с проектом
3. **Используйте поддомен** для приложения

---

## 🎯 РЕКОМЕНДУЕМОЕ РЕШЕНИЕ:

### Вариант A: Простой редирект (быстро)

1. **В файловом менеджере** перейдите в `/home/dantistcil/public_html`
2. **Создайте файл** `index.html` с редиректом (код выше)
3. **Сохраните**
4. **Откройте:** `http://dantist-cilinic.by` → перенаправит на приложение

### Вариант B: Полная настройка (правильно)

1. **Скопируйте папку build** в `/public_html`
2. **Создайте .htaccess** для проксирования API
3. **Сервер работает** на порту 3001 (уже запущен)

---

## 📋 Что делать сейчас:

### Вариант 1: Быстрый редирект

1. **В файловом менеджере** откройте `/home/dantistcil/public_html`
2. **Создайте файл** `index.html`
3. **Вставьте код редиректа** (из Решения 1, Вариант B)
4. **Сохраните**
5. **Откройте:** `http://dantist-cilinic.by`

### Вариант 2: Правильная настройка

1. **В терминале:**
   ```bash
   cp -r ~/dantist-cilinic.by/id_rsa/client/build/* /home/dantistcil/public_html/
   ```

2. **Создайте .htaccess** в `/public_html` (код из Решения 2)

   **Подробная инструкция:**
   
   **Способ A: Через файловый менеджер (проще)**
   
   1. В панели управления откройте **файловый менеджер**
   2. Перейдите в папку `/home/dantistcil/public_html`
   3. Нажмите **"Создать файл"** или **"New File"**
   4. Назовите файл: `.htaccess` (с точкой в начале!)
   5. Откройте файл для редактирования
   6. Вставьте этот код:
   ```apache
   RewriteEngine On
   
   # API запросы проксируем на сервер Node.js
   RewriteCond %{REQUEST_URI} ^/api
   RewriteRule ^(.*)$ http://localhost:3001/api/$1 [P,L]
   
   # Остальные запросы - на React приложение (index.html)
   RewriteCond %{REQUEST_FILENAME} !-f
   RewriteCond %{REQUEST_FILENAME} !-d
   RewriteRule . /index.html [L]
   ```
   7. **Сохраните файл**
   
   **Способ B: Через терминал**
   
   1. В терминале выполните:
   ```bash
   cd /home/dantistcil/public_html
   nano .htaccess
   ```
   2. Вставьте код (выше)
   3. Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

3. **Проверьте:** `http://dantist-cilinic.by`

   **Подробная инструкция:**
   
   **Шаг 1: Проверьте, что файлы скопированы**
   
   В терминале:
   ```bash
   ls -la /home/dantistcil/public_html/
   ```
   
   Должны увидеть:
   - Файл `index.html` (главный файл React приложения)
   - Папка `static` (CSS, JS файлы)
   - Файл `.htaccess` (если создали)
   
   **Шаг 2: Проверьте, что сервер запущен**
   
   В терминале:
   ```bash
   pm2 status
   ```
   
   Должно показать, что приложение `clinic` запущено (status: online)
   
   **Шаг 3: Откройте в браузере**
   
   1. На вашем компьютере откройте браузер
   2. Введите адрес: `http://dantist-cilinic.by`
   3. Нажмите Enter
   
   **Что должно произойти:**
   - Откроется интерфейс системы управления клиникой
   - Должны быть вкладки: "Записи", "Клиенты", "Услуги"
   - Можно создавать клиентов и записи
   
   **Если не открывается:**
   
   - Проверьте логи сервера:
     ```bash
     pm2 logs clinic
     ```
   - Проверьте, что файлы в `/public_html`:
     ```bash
     ls -la /home/dantistcil/public_html/
     ```
   - Попробуйте открыть с IP: `http://93.125.99.149`
   - Проверьте, что порт 3001 открыт (если используете IP с портом)
   
   **Если видите ошибку:**
   
   - "404 Not Found" - файлы не скопированы, повторите шаг 1
   - "Connection refused" - сервер не запущен, выполните: `pm2 restart clinic`
   - "Timeout" - порт закрыт, нужно открыть в панели управления

---

## 🔍 Проверка:

После настройки:

1. **Откройте:** `http://dantist-cilinic.by`
2. **Должно открыться ваше приложение!**

Если не работает:
- Проверьте, что файлы в `/public_html`
- Проверьте, что сервер запущен: `pm2 status`
- Проверьте логи: `pm2 logs clinic`

---

## 💡 Важно:

- **Домен всегда указывает на `/public_html`** (на shared hosting)
- **Нужно либо:**
  - Переместить файлы в `/public_html`
  - Или создать редирект из `/public_html` на приложение

**Попробуйте сначала простой редирект - это быстрее всего!** 😊

