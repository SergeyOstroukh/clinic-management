# 🐘 Настройка PostgreSQL на Railway (БЕЗ Docker)

## ⚠️ Важно: Docker НЕ нужен!

На Railway PostgreSQL - это **managed service** (управляемый сервис). Railway сам управляет базой данных, вам не нужно:
- ❌ Использовать Docker
- ❌ Устанавливать PostgreSQL вручную
- ❌ Настраивать контейнеры
- ❌ Делать миграции через Docker

---

## ✅ Что нужно сделать (3 простых шага):

### Шаг 1: Создать PostgreSQL базу данных на Railway

1. Откройте ваш проект на https://railway.app/
2. Нажмите **"+ New"** (или **"+ Add Service"**)
3. Выберите **"Database"** → **"Add PostgreSQL"**
4. Подождите ~30 секунд пока создастся база данных

**Готово!** Railway автоматически:
- ✅ Создаст PostgreSQL базу данных
- ✅ Настроит все параметры
- ✅ Добавит переменную `DATABASE_URL` в ваш Node.js сервис

---

### Шаг 2: Проверить, что DATABASE_URL установлен

1. Откройте ваш **Node.js сервис** (не PostgreSQL!)
2. Перейдите во вкладку **"Variables"**
3. Вы должны увидеть переменную `DATABASE_URL`

**Если DATABASE_URL НЕТ:**

1. Откройте вашу **PostgreSQL базу данных** на Railway
2. Перейдите в **"Connect"** (или **"Variables"**)
3. Найдите **"Postgres Connection URL"** или **"DATABASE_URL"**
4. Скопируйте значение (формат: `postgresql://user:password@host:port/database`)
5. Вернитесь к **Node.js сервису** → **"Variables"**
6. Нажмите **"+ New Variable"**
7. Добавьте:
   - **Name:** `DATABASE_URL`
   - **Value:** `скопированное значение`
8. Сохраните

---

### Шаг 3: Перезапустить сервис

1. Откройте ваш **Node.js сервис**
2. Перейдите во вкладку **"Deployments"**
3. Нажмите **"Redeploy"** (или просто подождите автоматического деплоя после изменений)

**Готово!** При запуске:
- ✅ Код автоматически подключится к PostgreSQL
- ✅ Создаст все таблицы (если их нет)
- ✅ Добавит пользователей по умолчанию

---

## 🔍 Как проверить, что все работает:

### 1. Проверьте логи Railway

Откройте логи вашего Node.js сервиса. Должны увидеть:

```
🐘 Используется PostgreSQL
📡 DATABASE_URL: postgresql://...
✅ Подключение к PostgreSQL успешно установлено
🔧 Инициализация базы данных PostgreSQL...
📊 Создание таблиц PostgreSQL...
   ✓ Все таблицы проверены
✅ Таблицы созданы/проверены
✅ Данные по умолчанию проверены
✅ База данных инициализирована
✅ База данных готова к работе
🚀 Сервер запущен на порту 3001
```

### 2. Проверьте в приложении

1. Откройте ваше приложение на Railway URL
2. Войдите как `Admin / admin`
3. Попробуйте:
   - Создать врача
   - Создать расписание
   - Создать запись

Если все работает - **готово!** ✅

---

## ❌ Если что-то не работает:

### Ошибка: "DATABASE_URL не установлен"

**Решение:** См. Шаг 2 выше

### Ошибка: "connection refused" или "timeout"

**Проверьте:**
1. PostgreSQL база данных запущена (зеленый индикатор на Railway)
2. DATABASE_URL правильный (скопирован полностью)
3. Нет опечаток в DATABASE_URL

### Ошибка: "relation does not exist"

**Причина:** Таблицы не создались

**Решение:**
1. Проверьте логи - должны быть ошибки при создании таблиц
2. Убедитесь, что DATABASE_URL правильный
3. Перезапустите сервис

---

## 🔄 Обновление базы данных

Если вы изменили структуру таблиц в коде:

1. **Закоммитьте изменения:**
   ```bash
   git add .
   git commit -m "Update database schema"
   git push
   ```

2. **Railway автоматически:**
   - Задеплоит новый код
   - Запустит инициализацию базы данных
   - Создаст новые таблицы/колонки (если используются `CREATE TABLE IF NOT EXISTS`)

**Важно:** Код использует `CREATE TABLE IF NOT EXISTS`, поэтому:
- ✅ Существующие таблицы не пересоздаются
- ✅ Новые таблицы создаются автоматически
- ✅ Данные не теряются

---

## 📊 Управление базой данных через Railway

### Просмотр данных:

1. Откройте PostgreSQL базу данных на Railway
2. Перейдите в **"Data"** или **"Query"**
3. Выполните SQL запросы:
   ```sql
   SELECT * FROM clients;
   SELECT * FROM doctors;
   SELECT * FROM doctor_schedules;
   ```

### Резервное копирование:

Railway автоматически делает бэкапы каждый день. Вы можете:
1. Открыть PostgreSQL базу данных
2. Перейти в **"Backups"**
3. Скачать бэкап при необходимости

---

## 🆚 Локально vs Прод

### Локально:
- Используется ваш локальный PostgreSQL (если установлен)
- Или SQLite (если PostgreSQL не установлен)
- DATABASE_URL из `.env` файла

### На проде (Railway):
- Используется PostgreSQL от Railway
- DATABASE_URL автоматически добавляется Railway
- Все управляется через веб-интерфейс Railway

**Никакого Docker не нужно!** 🎉

---

## ✅ Итоговый чеклист:

- [ ] PostgreSQL база данных создана на Railway
- [ ] DATABASE_URL установлен в Variables Node.js сервиса
- [ ] Сервис перезапущен
- [ ] В логах есть "✅ Подключение к PostgreSQL успешно установлено"
- [ ] В логах есть "✅ База данных инициализирована"
- [ ] Приложение работает (можно создать расписание/запись)

**Если все пункты выполнены - готово!** 🎉

---

## 📞 Нужна помощь?

Если что-то не работает:
1. Проверьте логи Railway (должны быть подробные сообщения об ошибках)
2. См. файл `PRODUCTION_DEBUG.md` для детальной диагностики
3. Убедитесь, что DATABASE_URL правильный и PostgreSQL запущен

