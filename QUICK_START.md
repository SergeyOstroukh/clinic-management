# ⚡ Быстрый старт - Развертывание в облаке

## 🎯 Цель: Работа из любой точки мира

> **Используете Hoster.by?** См. подробную инструкцию в [HOSTER_BY.md](HOSTER_BY.md)

## Вариант 1: Railway.app (5 минут, БЕСПЛАТНО) ⭐

### Шаг 1: Подготовка кода

1. **Создайте репозиторий на GitHub:**
   - Зарегистрируйтесь на https://github.com
   - Создайте новый репозиторий (например, `clinic-management`)
   - Загрузите код:
     ```bash
     git init
     git add .
     git commit -m "Initial commit"
     git remote add origin https://github.com/ваш-username/clinic-management.git
     git push -u origin main
     ```

### Шаг 2: Развертывание на Railway

1. **Войдите на Railway:**
   - Перейдите на https://railway.app
   - Нажмите "Login" и войдите через GitHub

2. **Создайте проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий `clinic-management`

3. **Настройте переменные окружения:**
   - В проекте нажмите на сервис
   - Перейдите в "Variables"
   - Добавьте:
     ```
     NODE_ENV=production
     ```

4. **Настройте деплой:**
   - Railway автоматически определит Node.js
   - Если нужно, в "Settings" → "Build & Deploy":
     - **Build Command:** `npm run install-all && npm run build`
     - **Start Command:** `cd server && npm start`

5. **Получите URL:**
   - Railway автоматически создаст URL типа: `https://clinic-management-production.up.railway.app`
   - Нажмите на URL или кнопку "Generate Domain" для красивого домена

6. **Готово!** 🎉
   - Откройте URL в браузере
   - Работайте из любой точки мира!

---

## Вариант 2: Render.com (5 минут, БЕСПЛАТНО)

### Шаг 1: Подготовка (то же, что выше)

Загрузите код на GitHub.

### Шаг 2: Развертывание на Render

1. **Войдите на Render:**
   - https://render.com
   - Войдите через GitHub

2. **Создайте Web Service:**
   - Нажмите "New" → "Web Service"
   - Подключите ваш репозиторий

3. **Настройки:**
   - **Name:** `clinic-management`
   - **Environment:** `Node`
   - **Build Command:** `npm run install-all && npm run build`
   - **Start Command:** `cd server && npm start`

4. **Переменные окружения:**
   - Добавьте:
     ```
     NODE_ENV=production
     PORT=10000
     ```

5. **Создайте сервис:**
   - Нажмите "Create Web Service"
   - Render автоматически развернет приложение
   - Получите URL: `https://clinic-management.onrender.com`

6. **Готово!** 🎉

---

## 📱 Использование

После развертывания:

1. **Откройте URL** в любом браузере
2. **Работайте из любой точки:**
   - Дома
   - В офисе
   - С телефона
   - С планшета

3. **Все данные синхронизируются** - изменения видны всем пользователям сразу

---

## 🔄 Обновление приложения

После изменений в коде:

1. **Закоммитьте изменения:**
   ```bash
   git add .
   git commit -m "Обновление"
   git push
   ```

2. **Railway/Render автоматически переразвернет** приложение

---

## 💡 Советы

- **Бесплатные тарифы** имеют ограничения (например, приложение "засыпает" после неактивности)
- **Для постоянной работы** рассмотрите платный тариф или VPS
- **Настройте резервное копирование** базы данных
- **Добавьте авторизацию** для безопасности (можно позже)

---

## 🆘 Проблемы?

**Приложение не запускается:**
- Проверьте логи в панели Railway/Render
- Убедитесь, что переменные окружения установлены
- Проверьте, что код загружен на GitHub

**База данных не работает:**
- На бесплатных тарифах база данных может сбрасываться при перезапуске
- Рассмотрите использование внешней БД (PostgreSQL) для продакшена

**Нужна помощь?**
- См. подробную инструкцию в `DEPLOY.md`
- Проверьте логи в панели управления хостинга

