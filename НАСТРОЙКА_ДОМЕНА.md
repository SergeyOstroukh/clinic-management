# 🌐 Настройка доступа через домен

## ❓ Вопрос: Почему домен должен открывать приложение?

**Ответ:** Домен сам по себе не знает, где ваше приложение! Нужно настроить связь между доменом и приложением.

---

## 🎯 Как это работает:

1. **Домен** (`dentist-clinic.by`) → указывает на IP сервера (`93.125.99.149`)
2. **Но сервер не знает**, что по этому домену нужно открывать ваше приложение
3. **Нужно настроить**, чтобы домен открывал ваше приложение на порту 3001

---

## ✅ РЕШЕНИЕ 1: Настроить через папку public_html (для shared hosting)

На shared hosting обычно есть папка `public_html` - это корневая папка для сайтов.

### Шаг 1: Найдите папку public_html

В файловом менеджере найдите папку `/home/dantistcil/public_html`

### Шаг 2: Создайте файл для перенаправления

Создайте файл `.htaccess` в папке `public_html`:

```apache
RewriteEngine On
RewriteRule ^(.*)$ http://localhost:3001/$1 [P,L]
```

Или создайте файл `index.html` с перенаправлением:

```html
<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="refresh" content="0; url=http://localhost:3001">
</head>
<body>
    <p>Перенаправление...</p>
</body>
</html>
```

**Но это не лучший способ!** Лучше использовать настройки в панели.

---

## ✅ РЕШЕНИЕ 2: Настроить через панель управления (РЕКОМЕНДУЕТСЯ)

### Вариант A: Через раздел "Домены"

1. **В панели управления найдите раздел "Домены"**
2. **Откройте настройки домена** `dentist-clinic.by`
3. **Найдите настройки:**
   - **"Корневая папка"** или **"Document Root"**
   - **"Настройки приложения"** или **"Application Settings"**
   - **"Обратный прокси"** или **"Reverse Proxy"**

4. **Настройте:**
   - Укажите, что домен должен проксировать запросы на `http://localhost:3001`
   - Или укажите путь к приложению

### Вариант B: Через раздел "Node.js" (если есть)

1. **Найдите раздел "Node.js"** или **"Node.js Selector"**
2. **Создайте новое приложение:**
   - **Домен:** `dentist-clinic.by`
   - **Путь:** `/home/dantistcil/dantist-cilinic.by/id_rsa/server`
   - **Файл запуска:** `index.js`
   - **Порт:** `3001` (или автоматический)

3. **Сохраните** - панель автоматически настроит доступ через домен

---

## ✅ РЕШЕНИЕ 3: Использовать IP с портом (проще всего)

Пока не настроен домен, используйте IP:

```
http://93.125.99.149:3001
```

Но для этого нужно открыть порт 3001 (см. предыдущие инструкции).

---

## ✅ РЕШЕНИЕ 4: Настроить поддомен

Создайте поддомен специально для приложения:

1. **В панели управления найдите "Поддомены"** или **"Subdomains"**
2. **Создайте поддомен:** `app.dentist-clinic.by`
3. **Настройте его на ваше приложение**

---

## 🎯 Что делать сейчас:

### Вариант 1: Настроить через панель (ЛУЧШЕ)

1. **В панели управления найдите раздел "Домены"**
2. **Откройте настройки домена** `dentist-clinic.by`
3. **Найдите опцию "Обратный прокси"** или **"Application"**
4. **Настройте на порт 3001**

### Вариант 2: Использовать IP (БЫСТРЕЕ)

1. **Откройте порт 3001** в панели (раздел "Безопасность" → "Файрвол")
2. **Используйте:** `http://93.125.99.149:3001`

### Вариант 3: Связаться с поддержкой

**Напишите в поддержку Hoster.by:**

"Здравствуйте! У меня есть домен dentist-clinic.by и Node.js приложение, запущенное на порту 3001. Как настроить, чтобы домен открывал это приложение?"

Они помогут с настройкой!

---

## 💡 Важно понимать:

- **Домен** = адрес (как адрес дома)
- **Приложение** = содержимое (как сам дом)
- **Нужно связать** адрес с содержимым!

Сейчас домен указывает на сервер, но не знает, что открывать. Нужно настроить связь!

---

## 📋 Чек-лист:

- [ ] Домен `dentist-clinic.by` активен ✅ (уже есть)
- [ ] Приложение запущено на порту 3001 ✅ (уже есть)
- [ ] Настроена связь домена с приложением ❌ (нужно сделать)
- [ ] Порт 3001 открыт ❌ (нужно проверить)

**Следующий шаг:** Настройте связь через панель управления или откройте порт!

