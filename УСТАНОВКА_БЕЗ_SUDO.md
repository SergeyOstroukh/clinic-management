# 🔧 Установка Node.js без sudo

## ❌ Проблема: `sudo: command not found`

На вашем хостинге нет команды `sudo`. Это нормально для shared hosting.

## ✅ РЕШЕНИЕ: Используйте NVM (Node Version Manager)

NVM позволяет установить Node.js без прав администратора!

---

## 📦 ШАГ 1: Установите NVM

В терминале выполните:

```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
```

Нажмите Enter и подождите...

---

## 🔄 ШАГ 2: Перезагрузите терминал

После установки NVM нужно перезагрузить терминал. В терминале в панели управления:

1. **Закройте терминал** (закройте вкладку или окно)
2. **Откройте терминал снова**
3. Или выполните:

```bash
source ~/.bashrc
```

---

## ✅ ШАГ 3: Проверьте NVM

```bash
nvm --version
```

Должно показать версию NVM (например: `0.39.0`)

---

## 📥 ШАГ 4: Установите Node.js через NVM

```bash
nvm install 18
```

Подождите, пока установится...

---

## ✅ ШАГ 5: Используйте установленный Node.js

```bash
nvm use 18
```

---

## 🔍 ШАГ 6: Проверьте установку

```bash
node --version
```

Должно показать: `v18.x.x`

```bash
npm --version
```

Должно показать версию npm.

---

## 🎯 Если NVM не работает

### Альтернатива: Проверьте, может Node.js уже установлен?

```bash
which node
node --version
```

Если показывает версию - отлично! Node.js уже установлен.

---

## 🆘 Если ничего не работает

На shared hosting (общий хостинг) может быть ограничение на установку программ.

**Варианты:**

1. **Спросите поддержку Hoster.by:**
   - "Как установить Node.js на моем аккаунте?"
   - "Нужен доступ к Node.js для запуска приложения"

2. **Используйте готовый Node.js** (если есть в панели):
   - В панели управления найдите раздел "Node.js" или "Node.js Selector"
   - Активируйте Node.js там

3. **Рассмотрите VPS** вместо shared hosting:
   - На VPS вы можете устанавливать что угодно
   - Shared hosting имеет ограничения

---

## 📋 Что делать дальше

После установки Node.js:

1. Установите зависимости проекта
2. Соберите React приложение
3. Запустите сервер

Но сначала нужно установить Node.js!

