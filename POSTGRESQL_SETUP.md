# Настройка PostgreSQL на Railway (Рекомендуемый способ)

## Преимущества PostgreSQL
- ✅ **Автоматические бэкапы** - Railway делает резервные копии
- ✅ **Надежность** - данные никогда не пропадут при деплое
- ✅ **Масштабируемость** - подходит для роста бизнеса
- ✅ **Managed database** - не нужно настраивать вручную

---

## Шаг 1: Создание PostgreSQL базы данных

1. Откройте ваш проект на Railway: https://railway.app/
2. Нажмите **"+ New"** или **"Add Service"**
3. Выберите **"Database"** → **"Add PostgreSQL"**
4. Railway автоматически создаст базу данных

---

## Шаг 2: Получение строки подключения

1. Откройте созданную PostgreSQL базу данных
2. Перейдите во вкладку **"Variables"** или **"Connect"**
3. Скопируйте значение переменной **`DATABASE_URL`**
   
   Оно будет выглядеть примерно так:
   ```
   postgresql://postgres:password@containers-us-west-xxx.railway.app:5432/railway
   ```

---

## Шаг 3: Настройка переменной окружения в вашем сервисе

1. Вернитесь к вашему основному сервису (Node.js приложение)
2. Перейдите во вкладку **"Variables"**
3. Добавьте новую переменную:
   - **Variable Name**: `DATABASE_URL`
   - **Value**: вставьте скопированную строку подключения из шага 2

**ВАЖНО:** Railway может автоматически связать сервисы. Если вы видите переменную `DATABASE_URL` уже добавленной автоматически - ничего делать не нужно!

---

## Шаг 4: Деплой приложения

1. Убедитесь, что все файлы добавлены в git:
```bash
git add .
git commit -m "Add PostgreSQL support"
git push
```

2. Railway автоматически задеплоит обновленное приложение
3. При первом запуске автоматически:
   - Создадутся все таблицы
   - Добавятся пользователи по умолчанию:
     * `Admin / admin` (superadmin)
     * `Administrator / administrator` (administrator)
     * `Doctor1 / doctor` (doctor)
   - Добавятся материалы по умолчанию
   - Импортируются услуги (если есть файл `services_data.js`)

---

## Шаг 5: Проверка работы

1. Откройте ваше приложение (Railway предоставит URL)
2. Войдите с учетными данными:
   - Логин: `Admin`
   - Пароль: `admin`
3. Попробуйте:
   - Создать клиента
   - Добавить услугу
   - Сделать запись
4. Сделайте тестовый деплой (измените что-то и push)
5. Проверьте, что данные остались на месте! ✅

---

## Локальная разработка (SQLite)

Для локальной разработки приложение автоматически использует SQLite:

```bash
# Запуск локально
npm run dev
```

При запуске без переменной `DATABASE_URL` приложение:
- Создаст файл `clinic.db` в папке `server/`
- Будет работать с SQLite

Это удобно для разработки, не нужно настраивать PostgreSQL локально!

---

## Миграция данных из SQLite в PostgreSQL

Если у вас уже есть данные в SQLite и вы хотите перенести их:

1. **Экспорт данных из SQLite:**
   - Используйте инструмент типа `sqlite3` CLI или DB Browser for SQLite
   - Экспортируйте данные в SQL или CSV формат

2. **Импорт в PostgreSQL:**
   - Подключитесь к PostgreSQL через Railway CLI или psql
   - Импортируйте данные

**Или используйте автоматический скрипт миграции:**

```bash
# Этот скрипт можно создать отдельно если нужно
node server/migrate-sqlite-to-postgres.js
```

---

## Мониторинг базы данных

Railway предоставляет:
- 📊 **Метрики использования** (CPU, Memory, Disk)
- 📈 **Графики производительности**
- 💾 **Автоматические бэкапы**
- 🔍 **Логи запросов**

Все это доступно в интерфейсе Railway для вашей PostgreSQL базы.

---

## Важные примечания

1. **Бесплатный план Railway:**
   - Включает 500MB хранилища для PostgreSQL
   - Этого достаточно для небольшой клиники (несколько тысяч записей)
   
2. **Безопасность:**
   - Railway автоматически настраивает SSL для подключения
   - База данных доступна только вашему приложению
   
3. **Бэкапы:**
   - Railway делает автоматические бэкапы каждый день
   - Можно восстановить данные через панель управления
   
4. **Обновление кода:**
   - При каждом push приложение перезапускается
   - База данных НЕ затрагивается - все данные сохраняются! ✅

---

## Решение проблем

### Ошибка подключения к БД
```
Error: connect ECONNREFUSED
```
**Решение:** Проверьте, что переменная `DATABASE_URL` правильно скопирована

### База данных пустая после деплоя
**Решение:** 
1. Проверьте логи Railway - должно быть сообщение "✅ База данных инициализирована"
2. Если нет, проверьте что `init-db.js` правильно выполнился

### Ошибка SSL
```
Error: self signed certificate
```
**Решение:** Это нормально для Railway. Код уже настроен с `rejectUnauthorized: false`

---

## Контакты и поддержка

- Railway документация: https://docs.railway.app/
- PostgreSQL документация: https://www.postgresql.org/docs/

**Готово! Теперь ваша клиника работает на надежной PostgreSQL базе данных! 🎉**

