# 🔧 Исправление ошибки 500 Internal Server Error

## ❌ Проблема: Internal Server Error

Сервер работает, но при открытии домена ошибка 500. Это проблема с конфигурацией `.htaccess` или Apache.

---

## ✅ РЕШЕНИЕ 1: Проверить логи ошибок

В терминале на сервере:

```bash
tail -50 /var/log/apache2/error.log
```

Или:

```bash
tail -50 ~/logs/error_log
```

Там будет указана причина ошибки.

---

## ✅ РЕШЕНИЕ 2: Упростить .htaccess (РЕКОМЕНДУЕТСЯ)

Возможно, проблема в проксировании. Попробуйте простой редирект.

### Шаг 1: Удалите старый .htaccess

В терминале:

```bash
rm /home/dantistcil/public_html/.htaccess
```

### Шаг 2: Создайте простой редирект

```bash
nano /home/dantistcil/public_html/.htaccess
```

Вставьте этот код:

```apache
Options -Indexes

<IfModule mod_rewrite.c>
  RewriteEngine On
  
  # Для React Router - все запросы на index.html
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

---

## ✅ РЕШЕНИЕ 3: Проверить, что файлы на месте

```bash
ls -la /home/dantistcil/public_html/
```

Должны быть:
- `index.html`
- Папка `static/`
- Файл `.htaccess`

Если файлов нет - скопируйте их:

```bash
cp -r ~/dantist-cilinic.by/id_rsa/client/build/* /home/dantistcil/public_html/
```

---

## ✅ РЕШЕНИЕ 4: Проверить права доступа

```bash
chmod 644 /home/dantistcil/public_html/.htaccess
chmod 644 /home/dantistcil/public_html/index.html
chmod 755 /home/dantistcil/public_html/
```

---

## ✅ РЕШЕНИЕ 5: Временно отключить .htaccess

Переименуйте файл для проверки:

```bash
mv /home/dantistcil/public_html/.htaccess /home/dantistcil/public_html/.htaccess.bak
```

Попробуйте открыть домен. Если открывается - проблема в `.htaccess`.

---

## 🎯 БЫСТРОЕ РЕШЕНИЕ (попробуйте сначала это)

### Шаг 1: Убедитесь, что файлы скопированы

```bash
ls -la /home/dantistcil/public_html/index.html
```

Если файла нет:

```bash
cp -r ~/dantist-cilinic.by/id_rsa/client/build/* /home/dantistcil/public_html/
```

### Шаг 2: Создайте простой .htaccess

```bash
cd /home/dantistcil/public_html
nano .htaccess
```

Вставьте:

```apache
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
```

Сохраните.

### Шаг 3: Проверьте

Откройте: `http://dantist-cilinic.by`

---

## 📋 Если все равно не работает

### Вариант A: Используйте IP с портом

Попробуйте открыть напрямую:

```
http://93.125.99.149:3001
```

Если открывается - значит сервер работает, проблема в настройке домена.

### Вариант B: Обратитесь в поддержку

Напишите в поддержку Hoster.by:

"Здравствуйте! У меня ошибка 500 при открытии домена dantist-cilinic.by. Node.js приложение работает на порту 3001. Помогите настроить доступ к приложению через домен."

---

## 🆘 Причины ошибки 500

1. **Синтаксическая ошибка в .htaccess** - самая частая причина
2. **Модуль mod_rewrite не включен** - включается в панели
3. **Модуль mod_proxy не поддерживается** - на shared hosting часто отключен
4. **Неправильные права доступа** - должно быть 644
5. **Файлы не скопированы** - нет index.html

---

## 💡 Рекомендация

На shared hosting проксирование через `.htaccess` может не работать из-за ограничений.

**Лучше использовать:**

1. Файлы React приложения в `/public_html`
2. Простой `.htaccess` для React Router (без проксирования)
3. API работает отдельно на порту 3001

**Но!** API не будет доступен через домен, только по IP с портом. Это ограничение shared hosting.

**Альтернатива:** Используйте VPS вместо shared hosting - там больше возможностей.

---

## ✅ Что делать прямо сейчас

1. Скопируйте файлы в public_html:
   ```bash
   cp -r ~/dantist-cilinic.by/id_rsa/client/build/* /home/dantistcil/public_html/
   ```

2. Создайте простой .htaccess (код выше)

3. Проверьте: `http://dantist-cilinic.by`

Начните с этого - это должно заработать.

