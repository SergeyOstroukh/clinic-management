{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "corepack npm run install-all && corepack npm run build"
  },
  "deploy": {
    "startCommand": "cd server && node index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

