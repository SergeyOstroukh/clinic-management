# 🔄 Обновление сайта на хостинге после изменений

## 🎯 Как обновлять изменения на хостинге

---

## 📝 Что изменяется

### 1. Изменения в коде frontend (React)

**Файлы:** `client/src/*` (App.js, App.css и т.д.)

**Что делать:**

1. **На вашем компьютере:**
   ```powershell
   cd D:\clinic\client
   npm run build
   ```

2. **Загрузите обновленную папку build на хостинг:**
   
   Через WinSCP:
   - Удалите старые файлы из `/home/dantistcil/public_html/`
   - Загрузите новые файлы из `D:\clinic\client\build\*`

   Или через SCP:
   ```powershell
   scp -r D:\clinic\client\build/* dantistcil@93.125.99.149:/home/dantistcil/public_html/
   ```

3. **Обновите браузер** (Ctrl+F5)

---

### 2. Изменения в коде backend (Node.js)

**Файлы:** `server/*` (index.js и т.д.)

**Что делать:**

1. **Загрузите измененные файлы на хостинг:**
   
   Через WinSCP:
   - Загрузите файлы в `/home/dantistcil/dantist-cilinic.by/id_rsa/server/`

   Или через SCP:
   ```powershell
   scp D:\clinic\server\index.js dantistcil@93.125.99.149:/home/dantistcil/dantist-cilinic.by/id_rsa/server/
   ```

2. **Перезапустите сервер:**
   ```bash
   pm2 restart clinic
   ```

3. **Проверьте:**
   ```bash
   pm2 logs clinic
   ```

---

### 3. Изменения в зависимостях (package.json)

**Что делать:**

1. Загрузите новый `package.json` на хостинг

2. В терминале на сервере:
   ```bash
   cd ~/dantist-cilinic.by/id_rsa/server
   npm install
   pm2 restart clinic
   ```

   Или для клиента:
   ```bash
   cd ~/dantist-cilinic.by/id_rsa/client
   npm install
   npm run build
   ```
   Затем загрузите новый build на хостинг.

---

## 🚀 Быстрый процесс обновления

### Для изменений в интерфейсе (React):

1. На компьютере:
   ```powershell
   cd D:\clinic\client
   npm run build
   ```

2. Загрузите папку `build` на хостинг (в `/public_html`)

3. Обновите браузер

---

### Для изменений в API (Node.js):

1. Загрузите измененные файлы на хостинг

2. На сервере:
   ```bash
   pm2 restart clinic
   ```

---

## 📦 Альтернатива: Через Git (РЕКОМЕНДУЕТСЯ для будущего)

### Один раз настроить Git:

1. **На вашем компьютере:**
   ```powershell
   cd D:\clinic
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/ваш-username/clinic.git
   git push -u origin main
   ```

2. **На сервере:**
   ```bash
   cd ~/dantist-cilinic.by/id_rsa
   git init
   git remote add origin https://github.com/ваш-username/clinic.git
   git pull origin main
   ```

### Затем для обновлений:

1. **На компьютере (после изменений):**
   ```powershell
   git add .
   git commit -m "Описание изменений"
   git push
   ```

2. **На сервере:**
   ```bash
   cd ~/dantist-cilinic.by/id_rsa
   git pull
   
   # Если изменили frontend:
   cd client && npm run build
   cp -r build/* /home/dantistcil/public_html/
   
   # Если изменили backend:
   pm2 restart clinic
   ```

---

## 🎯 Чек-лист обновления

### Frontend (React):
- [ ] Внесли изменения в `client/src/`
- [ ] Собрали: `npm run build`
- [ ] Загрузили `build/*` в `/public_html`
- [ ] Обновили браузер (Ctrl+F5)

### Backend (Node.js):
- [ ] Внесли изменения в `server/`
- [ ] Загрузили файлы на сервер
- [ ] Перезапустили: `pm2 restart clinic`
- [ ] Проверили логи: `pm2 logs clinic`

---

## 💡 Советы

1. **Тестируйте локально** перед загрузкой на хостинг:
   ```powershell
   npm run dev
   ```

2. **Делайте резервные копии** перед обновлением

3. **Используйте Git** для удобного управления версиями

4. **Проверяйте логи** после обновления backend:
   ```bash
   pm2 logs clinic
   ```

---

## 🆘 Если что-то пошло не так

1. **Откатите изменения** (загрузите старые файлы)
2. **Проверьте логи:**
   ```bash
   pm2 logs clinic --err
   ```
3. **Перезапустите сервер:**
   ```bash
   pm2 restart clinic
   ```

---

## ✅ Итог

**Для обновления frontend:**
- Соберите → Загрузите → Обновите браузер

**Для обновления backend:**
- Загрузите → Перезапустите сервер

**Просто и быстро!** 😊

