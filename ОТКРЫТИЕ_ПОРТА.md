# 🔓 Открытие порта для доступа к приложению

## ❌ Проблема: ERR_CONNECTION_TIMED_OUT

Порт 3001 закрыт файрволом или недоступен извне на shared hosting.

## ✅ РЕШЕНИЕ 1: Открыть порт в панели управления

### Шаг 1: Найдите настройки файрвола

В панели управления Hoster.by найдите:
- **"Файрвол"** или **"Firewall"**
- **"Порты"** или **"Ports"**
- **"Безопасность"** или **"Security"**

### Шаг 2: Откройте порт 3001

1. Добавьте правило:
   - **Порт:** `3001`
   - **Протокол:** `TCP`
   - **Действие:** `Разрешить` или `Allow`

2. Сохраните изменения

---

## ✅ РЕШЕНИЕ 2: Использовать стандартный порт (80 или 443)

На shared hosting обычно открыты только порты 80 (HTTP) и 443 (HTTPS).

### Вариант A: Изменить порт на 80

В терминале на сервере:

```bash
cd ~/dantist-cilinic.by/id_rsa/server
nano .env
```

Измените на:
```
NODE_ENV=production
PORT=80
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

Перезапустите:

```bash
pm2 restart clinic
```

Откройте: `http://93.125.99.149` (без порта)

**⚠️ Внимание:** Порт 80 может требовать прав root. Если не работает, попробуйте другой порт.

---

### Вариант B: Использовать папку public_html

На shared hosting обычно есть папка `public_html` для веб-сайтов.

1. **Создайте файл .htaccess** (для Apache) или настройте через панель

2. **Или используйте поддомен/подпапку** через панель управления

---

## ✅ РЕШЕНИЕ 3: Настроить через панель управления (РЕКОМЕНДУЕТСЯ)

### Через панель управления Hoster.by:

1. **Найдите раздел "Node.js"** или **"Node.js Selector"**
   - Многие панели имеют встроенную поддержку Node.js
   - Там можно указать папку приложения и порт

2. **Или настройте через "Веб-приложения"** или **"Web Applications"**
   - Создайте новое приложение
   - Укажите путь: `/home/dantistcil/dantist-cilinic.by/id_rsa/server`
   - Укажите файл запуска: `index.js`
   - Укажите порт: `3001` или используйте автоматический

3. **Или используйте "Обратный прокси"** (Reverse Proxy)
   - Настройте через панель, чтобы домен указывал на порт 3001

---

## ✅ РЕШЕНИЕ 4: Использовать другой порт (если 80 не работает)

Попробуйте порты, которые могут быть открыты:

```bash
# Попробуйте порт 8080
PORT=8080
```

Или:

```bash
# Попробуйте порт 3000
PORT=3000
```

---

## 🎯 Что делать сейчас:

### Вариант 1: Через панель управления (САМЫЙ ПРОСТОЙ)

1. Зайдите в панель управления Hoster.by
2. Найдите раздел **"Node.js"**, **"Веб-приложения"** или **"Файрвол"**
3. Откройте порт 3001 или настройте приложение через панель

### Вариант 2: Попробовать другой порт

В терминале:

```bash
cd ~/dantist-cilinic.by/id_rsa/server
nano .env
```

Попробуйте:
```
NODE_ENV=production
PORT=8080
```

Сохраните и перезапустите:

```bash
pm2 restart clinic
```

Попробуйте открыть: `http://93.125.99.149:8080`

---

## 📞 Если ничего не работает:

**Напишите в поддержку Hoster.by:**

"Здравствуйте! Я запустил Node.js приложение на порту 3001, но не могу получить к нему доступ из интернета. Как открыть порт или настроить доступ к приложению?"

Они помогут с настройкой!

---

## 💡 Рекомендация:

На shared hosting лучше всего использовать встроенные инструменты панели управления для Node.js приложений. Найдите раздел "Node.js" в панели - там обычно есть простой способ настроить доступ.

