# Система управления клиникой 🏥

Веб-приложение для управления клиникой с функциями:
- 📅 Запись клиентов на прием
- 👨‍⚕️ Управление врачами
- 💼 Каталог услуг с ценами
- 📦 Учет материалов
- 💰 Калькулятор оплаты и скидок
- 👥 Роли пользователей (Superadmin, Administrator, Doctor)
- 📊 Отчеты и статистика

---

## 🚀 Быстрый старт (Локальная разработка)

### 1. Установка зависимостей
```bash
npm run install-all
```

### 2. Запуск приложения
```bash
npm run dev
```

Приложение откроется на:
- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:3001

### 3. Вход в систему

**По умолчанию созданы 3 пользователя:**

| Роль | Логин | Пароль |
|------|-------|--------|
| Superadmin | Admin | admin |
| Administrator | Administrator | administrator |
| Doctor | Doctor1 | doctor |

---

## 💾 База данных

Приложение поддерживает **два типа баз данных**:

### SQLite (Локальная разработка)
- Автоматически используется при запуске без `DATABASE_URL`
- Файл базы: `server/clinic.db`
- Не требует настройки

### PostgreSQL (Продакшен на Railway)
- Используется при наличии переменной окружения `DATABASE_URL`
- Надежное хранение данных
- Автоматические бэкапы

---

## 🌐 Деплой на Railway

### Шаг 1: Создание PostgreSQL базы
1. Откройте проект на Railway
2. Нажмите **"+ New"** → **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически создаст переменную `DATABASE_URL`

### Шаг 2: Деплой кода
```bash
git add .
git commit -m "Deploy to Railway"
git push
```

Railway автоматически:
- Установит зависимости
- Создаст таблицы в PostgreSQL
- Добавит пользователей по умолчанию
- Запустит приложение

**Подробная инструкция**: см. `POSTGRESQL_SETUP.md`

---

## 📁 Структура проекта

```
clinic/
├── client/                 # React фронтенд
│   ├── src/
│   │   ├── pages/         # Страницы (Login, Doctors, etc.)
│   │   ├── widgets/       # Компоненты-виджеты
│   │   ├── shared/        # Утилиты и библиотеки
│   │   ├── App.js         # Главный компонент
│   │   └── App.css        # Стили
│   └── package.json
│
├── server/                 # Node.js бэкенд
│   ├── index.js           # Главный файл сервера
│   ├── database.js        # Модуль работы с БД
│   ├── init-db.js         # Инициализация схемы
│   ├── migrations/        # Миграции и данные
│   └── package.json
│
├── package.json           # Корневой package.json
├── README.md              # Этот файл
└── POSTGRESQL_SETUP.md    # Инструкция по настройке PostgreSQL
```

---

## 🔧 Доступные команды

### Разработка
```bash
npm run dev          # Запуск клиента и сервера одновременно
npm run client       # Только клиент (React)
npm run server       # Только сервер (Node.js)
```

### Установка
```bash
npm run install-all  # Установка всех зависимостей
```

### Продакшен
```bash
npm run build        # Сборка клиента для продакшена
npm start            # Запуск сервера в продакшен режиме
```

---

## 👥 Роли пользователей

### Superadmin
- Полный доступ ко всем функциям
- Управление врачами, услугами, материалами
- Редактирование цен
- Экспорт данных в Excel
- Просмотр статистики

### Administrator
- Создание записей на прием
- Применение скидок
- Просмотр клиентов, врачей, услуг
- НЕ видит статистику
- НЕ может редактировать услуги/материалы

### Doctor
- Видит только свои записи на выбранную дату
- Выбор услуг и материалов для пациента
- Постановка диагноза
- НЕ видит цены
- НЕ видит общую базу клиентов

---

## 🛠️ Технологии

**Frontend:**
- React 18
- Axios для API запросов
- date-fns для работы с датами
- CSS с градиентами и анимациями

**Backend:**
- Node.js + Express
- SQLite3 (локально)
- PostgreSQL (продакшен)
- CORS для кросс-доменных запросов

**Деплой:**
- Railway.app
- Автоматический CI/CD из GitHub

---

## 📝 Импорт услуг

Для импорта списка услуг создайте файл `server/migrations/services_data.js`:

```javascript
module.exports = [
  { name: 'Консультация терапевта', price: 30, category: 'Консультации' },
  { name: 'УЗИ брюшной полости', price: 45, category: 'УЗИ' },
  // ... остальные услуги
];
```

При первом запуске услуги автоматически импортируются.

---

## 🐛 Решение проблем

### Ошибка "Cannot find module"
```bash
npm run install-all
```

### Порт 3001 занят
Измените порт в `server/index.js` или остановите другой процесс

### База данных пустая на Railway
1. Проверьте логи Railway
2. Убедитесь что `DATABASE_URL` настроен правильно
3. Перезапустите деплой

### Данные пропадают при деплое
➡️ Используйте PostgreSQL! См. `POSTGRESQL_SETUP.md`

---

## 📚 Дополнительная документация

- `POSTGRESQL_SETUP.md` - Настройка PostgreSQL на Railway
- `server/index.js.backup` - Резервная копия старой версии сервера

---

## 📞 Контакты

Если у вас возникли вопросы или проблемы, создайте Issue в репозитории.

---

**Готово! Ваша система управления клиникой готова к использованию! 🎉**
