# 🏥 Система управления клиникой

Веб-приложение для управления клиникой с функциями записи клиентов, управления услугами и автоматического расчета стоимости.

## ✨ Функции

- 📅 **Запись клиентов** на определенную дату и время
- 💼 **Управление услугами** с ценами
- 🧮 **Автоматический расчет** стоимости услуг для каждой записи
- 👥 **Управление клиентами** (добавление, просмотр)
- 🌐 **Многопользовательский доступ** через браузер
- 📱 **Адаптивный дизайн** - работает на любых устройствах

## 🚀 Быстрый старт

> **Хотите работать из любой точки мира?** См. [QUICK_START.md](QUICK_START.md) - развертывание в облаке за 5 минут!

### Локальная разработка

**Требования:**
- Node.js (версия 14 или выше)
- npm или yarn

### Установка

1. **Установите все зависимости:**
   ```bash
   npm run install-all
   ```

   Или вручную:
   ```bash
   npm install
   cd server && npm install
   cd ../client && npm install
   ```

### Запуск

**Вариант 1: Запуск всего приложения одной командой**
```bash
npm run dev
```

**Вариант 2: Запуск по отдельности**

В одном терминале (Backend):
```bash
cd server
npm run dev
```

В другом терминале (Frontend):
```bash
cd client
npm start
```

### Доступ к приложению

После запуска откройте браузер и перейдите по адресу:
- **Frontend:** http://localhost:3000
- **Backend API:** http://localhost:3001/api

## 📖 Использование

### Запись клиента

1. Перейдите на вкладку **"Клиенты"**
2. Нажмите **"+ Новый клиент"**
3. Заполните данные клиента
4. Перейдите на вкладку **"Записи"**
5. Нажмите **"+ Новая запись"**
6. Выберите клиента, дату и время
7. Выберите услуги (можно несколько)
8. Система автоматически рассчитает общую стоимость
9. Сохраните запись

### Управление услугами

1. Перейдите на вкладку **"Услуги"**
2. Нажмите **"+ Новая услуга"**
3. Введите название, цену и описание
4. Услуги можно редактировать и удалять

### Автоматический расчет

При создании записи:
- Выберите одну или несколько услуг
- Укажите количество для каждой услуги
- Система автоматически рассчитает общую стоимость
- Итоговая сумма отображается в карточке записи

## 🗄️ База данных

Приложение использует SQLite базу данных, которая автоматически создается при первом запуске в файле `server/clinic.db`.

**Таблицы:**
- `clients` - клиенты
- `services` - услуги
- `appointments` - записи
- `appointment_services` - связь записей и услуг

## 🏗️ Архитектура приложения

**Как это работает:**
- **Сервер (Backend)** - обрабатывает запросы, работает с базой данных (запускается на одном компьютере)
- **Клиент (Frontend)** - открывается в браузере на любом компьютере, отправляет запросы на сервер

**Важно:** Клиентская часть действительно открывается в браузере! Сервер должен быть запущен на одном из компьютеров в сети.

## 🌐 Где разместить сервер?

### Вариант 1: Локальный сервер (рекомендуется для клиники) ⭐

**Сервер запускается на одном компьютере в сети клиники.**

**Как это работает:**
- Один компьютер = **сервер** (запускается backend)
- Остальные компьютеры = **клиенты** (открывают браузер и подключаются)

**Быстрая настройка:**

1. **На компьютере-сервере:**
   ```bash
   npm run install-all
   npm run dev
   ```

2. **Узнайте IP-адрес:**
   - Откройте cmd и введите: `ipconfig`
   - Найдите "IPv4-адрес" (например, `192.168.1.100`)

3. **На других компьютерах:**
   - Откройте браузер
   - Перейдите: `http://192.168.1.100:3000`
   - Готово!

4. **Настройте файрвол** (см. подробную инструкцию в `SETUP.md`)

**📖 Подробная инструкция:** См. файл `SETUP.md`

**Плюсы:**
- ✅ Данные остаются в клинике (безопасность)
- ✅ Не нужен интернет
- ✅ Бесплатно
- ✅ Быстрая работа в локальной сети

**Минусы:**
- ⚠️ Компьютер с сервером должен быть включен
- ⚠️ Нет доступа из интернета

### Вариант 2: Облачный хостинг (для доступа из интернета) ⭐ Рекомендуется

**Для работы из любой точки мира!**

**Бесплатные варианты:**
- **Railway.app** - 500 часов бесплатно, автодеплой из GitHub
- **Render.com** - бесплатный тариф, простой деплой
- **Fly.io** - бесплатный тариф, глобальная сеть

**Платные (более надежные):**
- **Hoster.by VPS** (Беларусь) - от ~15-20 BYN/месяц ⭐
- **VPS сервер** (Timeweb, Hetzner, DigitalOcean) - от 300₽/месяц

**📖 Подробные инструкции:**
- **Hoster.by:** См. `HOSTER_BY.md` (подробная инструкция для белорусского хостинга)
- **Другие платформы:** См. `DEPLOY.md`

**Плюсы:**
- ✅ Доступ из любого места (дом, офис, телефон)
- ✅ Не нужно держать компьютер включенным
- ✅ Автоматические обновления
- ✅ Бесплатные варианты доступны

**Минусы:**
- ⚠️ Нужен интернет для работы
- ⚠️ Данные в облаке (настройте безопасность)

### Быстрый деплой в облако (5 минут)

**Railway.app (рекомендуется):**

1. Создайте аккаунт на https://railway.app
2. Подключите GitHub репозиторий
3. Railway автоматически развернет приложение
4. Получите URL и работайте из любой точки!

**Подробнее:** См. `DEPLOY.md`

### Настройка для продакшена

Для постоянного использования рекомендуется:

1. **Развернуть на облачном хостинге** (см. `DEPLOY.md`)
2. **Использовать PM2** для автозапуска (на VPS):
   ```bash
   npm install -g pm2
   cd server
   pm2 start index.js --name clinic-server
   ```
3. **Настроить резервное копирование** базы данных
4. **Добавить авторизацию** для безопасности

## 🛠️ Технологии

- **Frontend:** React, CSS3
- **Backend:** Node.js, Express
- **База данных:** SQLite
- **HTTP клиент:** Axios

## 📝 Структура проекта

```
clinic/
├── server/          # Backend (Node.js + Express)
│   ├── index.js     # Основной файл сервера
│   └── clinic.db    # База данных SQLite (создается автоматически)
├── client/          # Frontend (React)
│   ├── src/
│   │   ├── App.js   # Главный компонент
│   │   └── App.css  # Стили
│   └── public/
└── README.md
```

## 🔧 API Endpoints

- `GET /api/clients` - Получить всех клиентов
- `POST /api/clients` - Создать клиента
- `GET /api/services` - Получить все услуги
- `POST /api/services` - Создать услугу
- `PUT /api/services/:id` - Обновить услугу
- `DELETE /api/services/:id` - Удалить услугу
- `GET /api/appointments` - Получить все записи
- `POST /api/appointments` - Создать запись
- `PUT /api/appointments/:id` - Обновить запись
- `DELETE /api/appointments/:id` - Удалить запись
- `GET /api/appointments/date/:date` - Получить записи на дату

## 📄 Лицензия

ISC

## 💡 Поддержка

При возникновении проблем проверьте:
- Запущен ли сервер на порту 3001
- Запущен ли клиент на порту 3000
- Открыты ли порты в файрволе
- Установлены ли все зависимости

