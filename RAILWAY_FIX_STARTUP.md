# 🔧 Исправление проблемы запуска сервера на Railway

## Проблема

Сервер не запускается на Railway. В логах видно:
```
npm error command sh -c cd server && npm start
npm error signal SIGTERM
```

## ✅ Что исправлено:

1. **Изменена команда запуска в `railway.json`:**
   - Было: `"startCommand": "npm start"`
   - Стало: `"startCommand": "cd server && node index.js"`
   - Это более надежная команда, которая напрямую запускает сервер

2. **Улучшена обработка ошибок подключения:**
   - Сервер не будет падать молча при ошибке подключения
   - Ошибки будут видны в логах

## 📋 Что нужно сделать:

### Шаг 1: Закоммитьте изменения

```bash
git add .
git commit -m "Fix Railway startup command"
git push
```

### Шаг 2: Проверьте DATABASE_URL на Railway

1. Откройте ваш проект на Railway
2. Откройте **Node.js сервис**
3. Перейдите в **"Variables"**
4. Убедитесь, что `DATABASE_URL` установлен

**Если DATABASE_URL НЕТ:**
1. Откройте **PostgreSQL базу данных** на Railway
2. Перейдите в **"Connect"**
3. Скопируйте `Connection String`
4. Вернитесь к Node.js сервису → **"Variables"**
5. Добавьте `DATABASE_URL` = `скопированное значение`

### Шаг 3: Перезапустите сервис

1. На Railway откройте ваш Node.js сервис
2. Перейдите в **"Deployments"**
3. Нажмите **"Redeploy"**
4. Подождите 2-3 минуты

### Шаг 4: Проверьте логи

Откройте логи вашего Node.js сервиса. Должны увидеть:

```
🐘 Используется PostgreSQL
📡 DATABASE_URL: postgresql://...
✅ Подключение к PostgreSQL успешно установлено
🔧 Инициализация базы данных PostgreSQL...
📊 Создание таблиц PostgreSQL...
   ✓ Все таблицы проверены
✅ Таблицы созданы/проверены
✅ Данные по умолчанию проверены
✅ База данных инициализирована
✅ База данных готова к работе
🚀 Сервер запущен на порту 3001
```

## ❌ Если все еще не работает:

### Ошибка: "DATABASE_URL не установлен"

**Решение:** См. Шаг 2 выше

### Ошибка: "connection refused" или "timeout"

**Проверьте:**
1. PostgreSQL база данных запущена (зеленый индикатор)
2. DATABASE_URL правильный
3. Нет опечаток в DATABASE_URL

### Ошибка: Сервер все еще падает с SIGTERM

**Проверьте:**
1. Логи Railway - должны быть подробные ошибки
2. Убедитесь, что код закоммичен и запушен
3. Попробуйте удалить и пересоздать сервис на Railway

## 🔍 Дополнительная диагностика:

Если проблема сохраняется, проверьте:

1. **Полные логи Railway:**
   - Откройте Node.js сервис → "Deployments" → последний деплой → "View Logs"
   - Скопируйте последние 50-100 строк

2. **Переменные окружения:**
   - Убедитесь, что `DATABASE_URL` установлен
   - Проверьте, что нет лишних пробелов или символов

3. **Статус PostgreSQL:**
   - Убедитесь, что PostgreSQL база данных запущена
   - Проверьте, что она не удалена

## ✅ После исправления:

После того как сервер запустится, проверьте:

1. ✅ В логах есть "✅ База данных инициализирована"
2. ✅ В логах есть "🚀 Сервер запущен на порту 3001"
3. ✅ Приложение открывается по Railway URL
4. ✅ Можно войти в систему
5. ✅ Можно создать расписание/запись

**Если все работает - готово!** 🎉

