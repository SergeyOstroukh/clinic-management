# 🚀 Развертывание на Hoster.by

Подробная инструкция по развертыванию системы управления клиникой на VPS от Hoster.by.

## 📋 Что нужно

1. **VPS/VDS сервер** на Hoster.by (не виртуальный хостинг!)
2. **Минимальные требования:**
   - 1 ГБ RAM (рекомендуется 2 ГБ)
   - 1 CPU
   - 10 ГБ диска
   - Ubuntu 20.04 или 22.04

## 🛒 Шаг 1: Заказ VPS

1. Зайдите на https://hoster.by
2. Выберите **"Виртуальные серверы"** (VPS/VDS)
3. Выберите подходящий тариф:
   - **Минимум:** 1 ГБ RAM, 1 CPU
   - **Рекомендуется:** 2 ГБ RAM для комфортной работы
4. Выберите ОС: **Ubuntu 22.04 LTS**
5. Оформите заказ и дождитесь активации

## 🔑 Шаг 2: Получение доступа

После активации вы получите:
- **IP-адрес сервера** (например, `185.123.45.67`)
- **Логин** (обычно `root`)
- **Пароль** (или инструкции по SSH ключу)

## 💻 Шаг 3: Подключение к серверу

### Windows

**Вариант A: PowerShell (встроен в Windows 10/11)**
```powershell
ssh root@ваш-ip-адрес
```

**Вариант B: PuTTY (если PowerShell не работает)**
1. Скачайте PuTTY: https://www.putty.org/
2. Запустите PuTTY
3. Введите:
   - **Host Name:** ваш-ip-адрес
   - **Port:** 22
   - **Connection type:** SSH
4. Нажмите "Open"
5. Введите логин: `root`
6. Введите пароль (при вводе пароль не отображается - это нормально)

### Mac/Linux
```bash
ssh root@ваш-ip-адрес
```

## ⚙️ Шаг 4: Настройка сервера

### 4.1. Обновление системы
```bash
apt update && apt upgrade -y
```

### 4.2. Установка Node.js
```bash
# Установка Node.js 18.x (LTS версия)
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt-get install -y nodejs

# Проверьте установку
node --version  # Должно показать v18.x.x
npm --version   # Должно показать 9.x.x
```

### 4.3. Установка PM2
```bash
npm install -g pm2
```

PM2 будет автоматически запускать приложение при перезагрузке сервера.

### 4.4. Установка Git (если будете использовать)
```bash
apt install git -y
```

## 📦 Шаг 5: Загрузка проекта

### Вариант A: Через Git (рекомендуется)

1. **Создайте репозиторий на GitHub:**
   - Загрузите код на GitHub
   - Получите URL репозитория

2. **На сервере:**
   ```bash
   cd /opt
   git clone https://github.com/ваш-username/clinic-management.git clinic
   cd clinic
   ```

### Вариант B: Через SCP (с вашего компьютера)

**Windows PowerShell:**
```powershell
scp -r D:\clinic root@ваш-ip:/opt/clinic
```

**Mac/Linux:**
```bash
scp -r ./clinic root@ваш-ip:/opt/clinic
```

### Вариант C: Через панель управления Hoster.by

1. Используйте файловый менеджер в панели управления
2. Загрузите файлы через FTP/SFTP клиент (FileZilla, WinSCP)

## 🔧 Шаг 6: Установка зависимостей

```bash
cd /opt/clinic
npm run install-all
npm run build
```

Это займет несколько минут.

## ⚙️ Шаг 7: Настройка переменных окружения

```bash
cd /opt/clinic/server
nano .env
```

Добавьте:
```
NODE_ENV=production
PORT=3001
```

Сохраните:
- `Ctrl+O` (сохранить)
- `Enter` (подтвердить имя файла)
- `Ctrl+X` (выйти)

## 🚀 Шаг 8: Запуск приложения

```bash
cd /opt/clinic/server
pm2 start index.js --name clinic
pm2 save
pm2 startup
```

**Проверьте работу:**
```bash
pm2 status
pm2 logs clinic
```

Вы должны увидеть:
```
🚀 Сервер запущен на порту 3001
```

## 🌐 Шаг 9: Доступ к приложению

### По IP-адресу:
Откройте в браузере: `http://ваш-ip-адрес:3001`

### Настройка файрвола (если нужно):
```bash
ufw allow 3001/tcp
ufw enable
```

## 🌍 Шаг 10: Настройка домена (опционально)

Если у вас есть домен на Hoster.by:

### 10.1. Настройка DNS

1. Войдите в панель управления доменом на Hoster.by
2. Перейдите в настройки DNS
3. Добавьте A-запись:
   - **Имя:** `@` (или `www`)
   - **Значение:** IP-адрес вашего VPS
   - **TTL:** 3600

### 10.2. Установка Nginx

```bash
apt install nginx -y
```

### 10.3. Создание конфигурации

```bash
nano /etc/nginx/sites-available/clinic
```

Добавьте:
```nginx
server {
    listen 80;
    server_name ваш-домен.by;

    location / {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### 10.4. Активация конфигурации

```bash
ln -s /etc/nginx/sites-available/clinic /etc/nginx/sites-enabled/
nginx -t  # Проверка конфигурации
systemctl restart nginx
```

### 10.5. Настройка SSL (HTTPS)

```bash
apt install certbot python3-certbot-nginx -y
certbot --nginx -d ваш-домен.by
```

Следуйте инструкциям. Certbot автоматически настроит SSL.

**Готово!** Откройте: `https://ваш-домен.by`

## 🔄 Управление приложением

### Полезные команды PM2:

```bash
pm2 status              # Статус приложения
pm2 logs clinic         # Просмотр логов (Ctrl+C для выхода)
pm2 restart clinic      # Перезапуск
pm2 stop clinic         # Остановка
pm2 delete clinic        # Удаление из PM2
pm2 monit               # Мониторинг в реальном времени
```

### Обновление приложения:

```bash
cd /opt/clinic
git pull                 # Если используете Git
npm run install-all
npm run build
pm2 restart clinic
```

## 💾 Резервное копирование

### Создание скрипта бэкапа:

```bash
nano /opt/clinic/backup.sh
```

Добавьте:
```bash
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/opt/clinic/backups"
mkdir -p $BACKUP_DIR
cp /opt/clinic/server/clinic.db $BACKUP_DIR/clinic_$DATE.db
# Удаляем старые бэкапы (старше 30 дней)
find $BACKUP_DIR -name "clinic_*.db" -mtime +30 -delete
echo "Backup created: clinic_$DATE.db"
```

Сделайте исполняемым:
```bash
chmod +x /opt/clinic/backup.sh
```

### Автоматический бэкап (каждый день в 2:00):

```bash
crontab -e
```

Добавьте строку:
```
0 2 * * * /opt/clinic/backup.sh
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

## 🔒 Безопасность

### Рекомендации:

1. **Измените пароль root:**
   ```bash
   passwd
   ```

2. **Создайте отдельного пользователя** (опционально):
   ```bash
   adduser clinic-user
   usermod -aG sudo clinic-user
   ```

3. **Настройте SSH ключи** вместо пароля (опционально)

4. **Регулярно обновляйте систему:**
   ```bash
   apt update && apt upgrade -y
   ```

## 🆘 Решение проблем

### Приложение не запускается:

```bash
# Проверьте логи
pm2 logs clinic

# Проверьте, что порт свободен
netstat -tulpn | grep 3001

# Проверьте права доступа
ls -la /opt/clinic/server/
```

### База данных не создается:

```bash
# Проверьте права на папку
chmod 755 /opt/clinic/server
chown -R root:root /opt/clinic/server
```

### Не могу подключиться:

1. Проверьте IP-адрес
2. Проверьте файрвол на сервере
3. Проверьте файрвол в панели Hoster.by

### Порт занят:

```bash
# Найдите процесс, использующий порт
lsof -i :3001

# Или измените порт в .env файле
```

## 📞 Поддержка

- **Hoster.by поддержка:** https://hoster.by/support
- **Документация PM2:** https://pm2.keymetrics.io/docs/
- **Документация Node.js:** https://nodejs.org/docs/

## ✅ Чек-лист развертывания

- [ ] VPS заказан и активирован
- [ ] Подключение по SSH работает
- [ ] Node.js установлен
- [ ] PM2 установлен
- [ ] Проект загружен на сервер
- [ ] Зависимости установлены
- [ ] Приложение собрано (`npm run build`)
- [ ] Файл .env создан
- [ ] Приложение запущено через PM2
- [ ] Приложение доступно по IP
- [ ] (Опционально) Домен настроен
- [ ] (Опционально) SSL сертификат установлен
- [ ] Резервное копирование настроено

**Готово! Ваше приложение работает из любой точки мира!** 🎉

