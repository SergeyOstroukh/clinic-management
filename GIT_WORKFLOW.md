# 🌿 Git Workflow: Ветки и Pull Requests

## ✅ Рекомендуемый подход

Использование веток и Pull Requests - это правильный подход для разработки! Это позволяет:
- ✅ Тестировать изменения перед мерджем в main
- ✅ Делать code review
- ✅ Откатывать изменения при необходимости
- ✅ Ведет историю изменений

---

## 🔄 Как работает деплой на Railway

### Автоматический деплой

Railway **автоматически деплоит** из ветки `main` (или `master`), если:
1. ✅ Railway подключен к вашему GitHub репозиторию
2. ✅ Настроен автоматический деплой из `main` ветки
3. ✅ Происходит push в `main` (включая мердж через PR)

### Что происходит при мердже PR в main:

1. Вы создаете ветку: `git checkout -b feature/fix-appointments`
2. Делаете изменения и коммиты
3. Пушите ветку: `git push origin feature/fix-appointments`
4. Создаете Pull Request на GitHub
5. После мерджа PR в `main`:
   - ✅ Railway автоматически обнаружит изменения
   - ✅ Запустит сборку (`npm run install-all && npm run build`)
   - ✅ Задеплоит новый код
   - ✅ Перезапустит сервер

**Важно:** Railway деплоит только из `main` ветки!

---

## 📋 Пошаговый workflow

### Шаг 1: Создать новую ветку

```bash
# Переключиться на main и обновить
git checkout main
git pull origin main

# Создать новую ветку
git checkout -b feature/название-фичи
# Например:
git checkout -b feature/fix-appointment-dates
git checkout -b feature/add-new-service
git checkout -b bugfix/schedule-display
```

### Шаг 2: Делать изменения

```bash
# Делайте изменения в коде
# ...

# Коммитьте изменения
git add .
git commit -m "Fix appointment date format normalization"
```

### Шаг 3: Пушить ветку на GitHub

```bash
# Первый раз (создает ветку на GitHub)
git push -u origin feature/название-фичи

# Последующие разы
git push
```

### Шаг 4: Создать Pull Request на GitHub

1. Откройте ваш репозиторий на GitHub
2. Нажмите **"Compare & pull request"** (появится после push)
3. Заполните:
   - **Title:** Краткое описание изменений
   - **Description:** Подробное описание того, что изменилось
4. Нажмите **"Create pull request"**

### Шаг 5: Code Review (опционально)

- Проверьте изменения в PR
- Обсудите с командой (если есть)
- Исправьте замечания (если есть)

### Шаг 6: Мердж в main

1. Нажмите **"Merge pull request"** на GitHub
2. Подтвердите мердж
3. Railway автоматически задеплоит изменения!

---

## 🔍 Проверка настройки Railway

### Убедитесь, что Railway подключен к GitHub:

1. Откройте ваш проект на https://railway.app/
2. Откройте ваш **Node.js сервис**
3. Перейдите в **"Settings"** → **"Source"**
4. Должно быть:
   - ✅ **Repository:** ваш GitHub репозиторий
   - ✅ **Branch:** `main` (или `master`)
   - ✅ **Auto Deploy:** включен

### Если Auto Deploy не включен:

1. Включите **"Auto Deploy"**
2. Убедитесь, что выбрана ветка `main`
3. Сохраните настройки

---

## 🎯 Примеры названий веток

### Feature (новая функциональность):
```
feature/add-payment-system
feature/export-to-excel
feature/email-notifications
```

### Bugfix (исправление ошибок):
```
bugfix/appointment-date-format
bugfix/schedule-display
bugfix/login-error
```

### Hotfix (срочное исправление):
```
hotfix/critical-security-fix
hotfix/database-connection
```

### Refactor (рефакторинг):
```
refactor/database-queries
refactor/api-structure
```

---

## ⚠️ Важные моменты

### 1. Всегда обновляйте main перед созданием ветки:

```bash
git checkout main
git pull origin main
git checkout -b feature/new-feature
```

### 2. Регулярно синхронизируйте ветку с main:

```bash
# В вашей feature ветке
git checkout feature/your-branch
git merge main
# Или используйте rebase (если знаете как)
git rebase main
```

### 3. Не мерджите в main напрямую:

❌ **Плохо:**
```bash
git checkout main
git merge feature/your-branch
git push origin main
```

✅ **Хорошо:**
- Создайте Pull Request на GitHub
- Мерджьте через интерфейс GitHub

### 4. Удаляйте ветки после мерджа:

После успешного мерджа PR:
```bash
# Удалить локальную ветку
git checkout main
git branch -d feature/your-branch

# Удалить ветку на GitHub (можно через интерфейс или):
git push origin --delete feature/your-branch
```

---

## 🚀 Быстрый старт для новой фичи

```bash
# 1. Обновить main
git checkout main
git pull origin main

# 2. Создать ветку
git checkout -b feature/my-new-feature

# 3. Делать изменения и коммиты
# ... редактируете файлы ...
git add .
git commit -m "Add new feature"

# 4. Пушить ветку
git push -u origin feature/my-new-feature

# 5. Создать PR на GitHub
# 6. После мерджа - Railway автоматически задеплоит!
```

---

## 🔄 Что происходит при деплое

После мерджа PR в `main`:

1. **GitHub:** Обновляется `main` ветка
2. **Railway:** Обнаруживает изменения (через webhook)
3. **Railway:** Запускает сборку:
   ```bash
   npm run install-all && npm run build
   ```
4. **Railway:** Запускает сервер:
   ```bash
   cd server && node index.js
   ```
5. **Railway:** Инициализирует базу данных
6. **Готово!** ✅

---

## 📊 Проверка деплоя

После мерджа PR:

1. Откройте Railway → ваш Node.js сервис
2. Перейдите в **"Deployments"**
3. Должен появиться новый деплой со статусом **"Deployed"**
4. Проверьте логи - должны быть сообщения об успешной инициализации

---

## ✅ Итог

**Да, деплой будет автоматически запускаться!** 🎉

Просто:
1. Создайте ветку
2. Сделайте изменения
3. Создайте Pull Request
4. Мерджьте в `main`
5. Railway автоматически задеплоит

**Никаких дополнительных действий не требуется!**

