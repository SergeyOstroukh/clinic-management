# 🔍 Диагностика проблем на проде

## Проблема: Локально работает, на проде не работает

### Возможные причины:

1. **DATABASE_URL не установлен или неправильный**
2. **База данных не инициализирована (таблицы не созданы)**
3. **Ошибки подключения к базе данных**
4. **Проблемы с SSL подключением**

---

## 📋 Шаг 1: Проверка DATABASE_URL на Railway

1. Откройте ваш проект на https://railway.app/
2. Откройте ваш **Node.js сервис**
3. Перейдите во вкладку **"Variables"**
4. Проверьте наличие переменной `DATABASE_URL`
5. Значение должно начинаться с `postgresql://` или `postgres://`

**Если DATABASE_URL НЕТ:**
1. Откройте вашу PostgreSQL базу данных на Railway
2. Перейдите в **"Connect"**
3. Скопируйте значение `Connection String` (формат `postgresql://...`)
4. Вернитесь к Node.js сервису → **"Variables"**
5. Добавьте новую переменную:
   - **Name:** `DATABASE_URL`
   - **Value:** `скопированное значение`
6. Сохраните и перезапустите сервис

---

## 📋 Шаг 2: Проверка логов Railway

1. Откройте ваш Node.js сервис на Railway
2. Перейдите во вкладку **"Deployments"**
3. Откройте последний деплой
4. Откройте **"View Logs"**

### ✅ Что должно быть в логах (если все ОК):

```
🐘 Используется PostgreSQL
📡 DATABASE_URL: postgresql://...
✅ Подключение к PostgreSQL успешно установлено
🔧 Инициализация базы данных PostgreSQL...
📊 Создание таблиц PostgreSQL...
✅ База данных инициализирована
✅ База данных готова к работе
🚀 Сервер запущен на порту 3001
```

### ❌ Что может быть в логах (если есть проблемы):

**Ошибка 1: DATABASE_URL не установлен**
```
❌ ОШИБКА: DATABASE_URL не установлен!
   Установите переменную окружения DATABASE_URL
```
**Решение:** См. Шаг 1

**Ошибка 2: Не удается подключиться**
```
❌ Ошибка подключения к PostgreSQL: connection refused
```
**Решение:** 
- Проверьте, что PostgreSQL база данных запущена на Railway
- Проверьте правильность DATABASE_URL
- Убедитесь, что база данных не удалена

**Ошибка 3: Таблицы не создаются**
```
❌ Ошибка инициализации базы данных: relation "clients" already exists
```
**Решение:** Это нормально, таблицы уже существуют. Проверьте другие ошибки.

**Ошибка 4: Ошибки при запросах**
```
❌ PostgreSQL query error: relation "doctor_schedules" does not exist
```
**Решение:** Таблицы не созданы. Проверьте логи инициализации.

---

## 📋 Шаг 3: Проверка таблиц в базе данных

Если у вас есть доступ к базе данных через Railway CLI или веб-интерфейс:

```sql
-- Проверьте наличие таблиц
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public';

-- Должны быть таблицы:
-- - clients
-- - doctors
-- - services
-- - appointments
-- - doctor_schedules
-- - doctor_specific_dates
-- - appointment_services
-- - materials
-- - appointment_materials
```

---

## 📋 Шаг 4: Ручная проверка подключения

Если у вас установлен Railway CLI:

```bash
# Подключитесь к базе данных
railway connect postgres

# Проверьте таблицы
\dt

# Проверьте данные
SELECT COUNT(*) FROM clients;
SELECT COUNT(*) FROM doctors;
SELECT COUNT(*) FROM doctor_schedules;
```

---

## 📋 Шаг 5: Проверка API endpoints

Откройте ваше приложение на проде и проверьте в консоли браузера (F12):

1. **Проверка врачей:**
   - Откройте Network tab
   - Перейдите на страницу врачей
   - Проверьте запрос `GET /api/doctors`
   - Должен вернуть статус 200 и массив врачей

2. **Проверка расписания:**
   - Откройте страницу расписания
   - Проверьте запрос `GET /api/schedules`
   - Должен вернуть статус 200 и массив расписаний

3. **Создание расписания:**
   - Попробуйте создать расписание
   - Проверьте запрос `POST /api/schedules`
   - Если ошибка 500, проверьте логи Railway

---

## 🔧 Частые проблемы и решения

### Проблема: "Cannot read property 'id' of undefined"

**Причина:** Запрос к базе данных возвращает пустой результат, но код ожидает объект.

**Решение:** Проверьте логи Railway на наличие ошибок SQL запросов.

### Проблема: "relation does not exist"

**Причина:** Таблицы не созданы в базе данных.

**Решение:** 
1. Проверьте логи инициализации
2. Если таблицы не создались, удалите базу данных и создайте заново
3. Или выполните миграции вручную

### Проблема: Расписание создается, но не отображается

**Причина:** 
- Ошибка в запросе получения расписания
- Проблема с фильтрацией по `doctor_id`

**Решение:**
1. Проверьте логи Railway на наличие ошибок
2. Проверьте запрос `GET /api/schedules?doctor_id=X` в Network tab
3. Убедитесь, что `doctor_id` передается правильно

### Проблема: Записи не создаются

**Причина:** Ошибка при вставке данных в базу.

**Решение:**
1. Проверьте логи Railway
2. Проверьте, что все обязательные поля заполнены
3. Проверьте формат даты (должен быть `YYYY-MM-DD HH:MM:SS`)

---

## 📞 Если ничего не помогло

1. **Скопируйте полные логи Railway** (последние 100-200 строк)
2. **Скопируйте ошибки из консоли браузера** (F12 → Console)
3. **Скопируйте ошибки из Network tab** (F12 → Network → запрос с ошибкой)
4. Проверьте, что:
   - DATABASE_URL установлен
   - PostgreSQL база данных запущена
   - Все таблицы созданы
   - Нет ошибок в логах сервера

---

## ✅ Чеклист перед деплоем

- [ ] DATABASE_URL установлен в Railway Variables
- [ ] PostgreSQL база данных создана и запущена
- [ ] Локально все работает
- [ ] Код закоммичен и запушен
- [ ] Railway успешно задеплоил (статус "Deployed")
- [ ] В логах нет ошибок подключения
- [ ] В логах есть "✅ База данных инициализирована"
- [ ] В логах есть "✅ Подключение к PostgreSQL успешно установлено"

---

**После исправления проблем, перезапустите сервис на Railway!**

