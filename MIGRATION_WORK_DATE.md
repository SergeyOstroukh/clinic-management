# 🔄 Автоматическая миграция work_date

## Проблема

На проде в таблице `doctor_specific_dates` колонка `work_date` может быть:
- Старого типа `DATE` (вместо `VARCHAR(50)`)
- Или иметь неправильный формат (например, с временем `2025-11-18T10:00:00` вместо `2025-11-18`)

Это приводит к тому, что расписание не работает правильно.

## ✅ Решение

Добавлена **автоматическая миграция**, которая запускается при каждом старте сервера и:

1. ✅ Проверяет тип колонки `work_date`
2. ✅ Если тип `DATE` - конвертирует в `VARCHAR(50)` в формате `YYYY-MM-DD`
3. ✅ Если уже `VARCHAR` - проверяет формат данных и исправляет неправильные записи
4. ✅ Убирает время из дат (если есть)
5. ✅ Приводит все даты к формату `YYYY-MM-DD`

## 📋 Что нужно сделать:

### Шаг 1: Закоммитьте изменения

```bash
git add .
git commit -m "Add automatic work_date migration"
git push
```

### Шаг 2: Подождите деплоя на Railway

Railway автоматически:
- Задеплоит новый код
- Перезапустит сервер
- Запустит миграцию при старте

### Шаг 3: Проверьте логи Railway

Откройте логи вашего Node.js сервиса. Должны увидеть:

```
🔧 Инициализация базы данных PostgreSQL...
📊 Создание таблиц PostgreSQL...
   ✓ Все таблицы проверены
✅ Таблицы созданы/проверены
   🔄 Миграция work_date: DATE -> VARCHAR(50)...
   ✅ Миграция типа колонки завершена
✅ Миграция дат проверена
```

Или если формат уже правильный:

```
   🔍 Проверка формата данных work_date...
   ✅ Формат данных правильный
✅ Миграция дат проверена
```

### Шаг 4: Проверьте расписание

1. Откройте ваше приложение на Railway
2. Войдите в систему
3. Перейдите в раздел расписания
4. Попробуйте:
   - Создать новое расписание
   - Просмотреть существующее расписание
   - Создать запись на время из расписания

**Если все работает - готово!** ✅

## 🔍 Что делает миграция:

### Случай 1: Колонка типа DATE

```sql
-- Создает временную колонку
ALTER TABLE doctor_specific_dates ADD COLUMN work_date_new VARCHAR(50);

-- Конвертирует DATE в строку YYYY-MM-DD
UPDATE doctor_specific_dates 
SET work_date_new = TO_CHAR(work_date, 'YYYY-MM-DD');

-- Удаляет старую колонку
ALTER TABLE doctor_specific_dates DROP COLUMN work_date;

-- Переименовывает новую колонку
ALTER TABLE doctor_specific_dates RENAME COLUMN work_date_new TO work_date;
```

### Случай 2: Колонка уже VARCHAR, но неправильный формат

Исправляет записи вида:
- `2025-11-18T10:00:00` → `2025-11-18`
- `2025-11-18 10:00:00` → `2025-11-18`
- Любые другие форматы → `YYYY-MM-DD`

## ⚠️ Важно:

- ✅ Миграция **безопасна** - не удаляет данные
- ✅ Миграция **идемпотентна** - можно запускать многократно
- ✅ Миграция **автоматическая** - запускается при каждом старте сервера
- ✅ Если миграция не удалась - сервер продолжит работу (не упадет)

## ❌ Если миграция не сработала:

### Проблема: В логах ошибка миграции

**Решение:**
1. Проверьте логи Railway - должны быть подробные ошибки
2. Убедитесь, что у базы данных есть права на ALTER TABLE
3. Попробуйте запустить миграцию вручную (см. ниже)

### Проблема: Расписание все еще не работает

**Решение:**
1. Проверьте логи - должна быть информация о миграции
2. Проверьте формат данных в базе:
   ```sql
   SELECT id, work_date FROM doctor_specific_dates LIMIT 5;
   ```
   Должен быть формат `YYYY-MM-DD` (например, `2025-11-18`)

## 🔧 Ручной запуск миграции (если нужно):

Если автоматическая миграция не сработала, можно запустить вручную:

### Через Railway CLI:

```bash
# Установите Railway CLI (если нет)
npm install -g @railway/cli

# Войдите в Railway
railway login

# Подключитесь к базе данных
railway connect postgres

# Выполните миграцию
node server/migrate-work-date.js
```

### Через веб-интерфейс Railway:

1. Откройте PostgreSQL базу данных на Railway
2. Перейдите в **"Data"** → **"Query"**
3. Выполните SQL команды из миграции (см. код выше)

## ✅ После миграции:

После успешной миграции:
- ✅ Все даты в формате `YYYY-MM-DD`
- ✅ Расписание работает правильно
- ✅ Можно создавать новые расписания
- ✅ Можно создавать записи на время из расписания

**Готово!** 🎉

