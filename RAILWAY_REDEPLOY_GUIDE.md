# 🔄 Что делать после загрузки нового кода на GitHub

## ✅ Хорошая новость: Обычно ничего делать не нужно!

Railway **автоматически**:
1. ✅ Обнаруживает новый код на GitHub
2. ✅ Запускает сборку (`npm run install-all && npm run build`)
3. ✅ Перезапускает сервер
4. ✅ При перезапуске сервера автоматически запускается инициализация базы данных

---

## 🔍 Как проверить, что Railway задеплоил новый код:

### 1. Проверьте статус деплоя на Railway

1. Откройте ваш проект на https://railway.app/
2. Откройте ваш **Node.js сервис**
3. Перейдите во вкладку **"Deployments"**
4. Должен быть новый деплой со статусом **"Deployed"** (зеленый) или **"Building"**

**Если статус "Failed":**
- Нажмите на деплой
- Откройте **"View Logs"**
- Проверьте ошибки

### 2. Проверьте логи после деплоя

Откройте логи вашего Node.js сервиса. Должны увидеть:

```
🐘 Используется PostgreSQL
📡 DATABASE_URL: postgresql://...
✅ Подключение к PostgreSQL успешно установлено
🔧 Инициализация базы данных PostgreSQL...
📊 Создание таблиц PostgreSQL...
   ✓ Все таблицы проверены
✅ Таблицы созданы/проверены
✅ Данные по умолчанию проверены
✅ База данных инициализирована
✅ База данных готова к работе
🚀 Сервер запущен на порту 3001
```

**Если видите эти сообщения - всё работает!** ✅

---

## ⚠️ Когда нужно перезапустить вручную:

### Случай 1: Railway не задеплоил автоматически

**Признаки:**
- В "Deployments" нет нового деплоя
- Логи старые (не обновляются)

**Решение:**
1. Откройте ваш Node.js сервис на Railway
2. Перейдите во вкладку **"Deployments"**
3. Нажмите **"Redeploy"** (или **"Deploy"**)
4. Подождите 2-3 минуты

### Случай 2: Изменили структуру базы данных

**Если вы добавили новые таблицы или колонки:**

Код использует `CREATE TABLE IF NOT EXISTS`, поэтому:
- ✅ Новые таблицы создадутся автоматически
- ✅ Существующие таблицы не пересоздадутся
- ⚠️ **НО:** Новые колонки в существующих таблицах НЕ добавятся автоматически!

**Если нужно добавить колонки:**
1. Создайте миграцию SQL
2. Или перезапустите сервис (но это не поможет, если код не создает колонки)

**Решение для новых колонок:**
- Используйте `ALTER TABLE` в миграциях
- Или добавьте проверку в код инициализации

### Случай 3: База данных "сломалась" или нужно пересоздать

**Если нужно полностью пересоздать базу данных:**

1. **ВАЖНО:** Это удалит все данные!
2. На Railway:
   - Откройте PostgreSQL базу данных
   - Нажмите **"Settings"** → **"Delete"**
   - Создайте новую PostgreSQL базу данных
   - Скопируйте новый DATABASE_URL в Node.js сервис
   - Перезапустите Node.js сервис

---

## 🔄 Пошаговая инструкция после git push:

### Шаг 1: Закоммитьте и запушьте код

```bash
git add .
git commit -m "Update database initialization"
git push
```

### Шаг 2: Подождите 2-3 минуты

Railway автоматически:
- Обнаружит изменения
- Запустит сборку
- Задеплоит новый код

### Шаг 3: Проверьте статус деплоя

1. Откройте Railway → ваш Node.js сервис → "Deployments"
2. Должен быть новый деплой со статусом "Deployed"

### Шаг 4: Проверьте логи

1. Откройте логи вашего Node.js сервиса
2. Убедитесь, что видите:
   - `✅ Подключение к PostgreSQL успешно установлено`
   - `✅ База данных инициализирована`

### Шаг 5: Проверьте в приложении

1. Откройте ваше приложение на Railway URL
2. Попробуйте:
   - Войти в систему
   - Создать расписание
   - Создать запись

**Если всё работает - готово!** ✅

---

## 🚨 Если что-то не работает:

### Проблема: Railway не задеплоил автоматически

**Решение:**
1. Проверьте, что код действительно запушен на GitHub
2. Проверьте настройки Railway (должен быть подключен репозиторий)
3. Нажмите "Redeploy" вручную

### Проблема: В логах ошибки подключения к базе

**Решение:**
1. Проверьте, что PostgreSQL база данных запущена (зеленый индикатор)
2. Проверьте, что DATABASE_URL правильный
3. Перезапустите Node.js сервис

### Проблема: Таблицы не создаются

**Решение:**
1. Проверьте логи - должны быть ошибки при создании таблиц
2. Убедитесь, что DATABASE_URL правильный
3. Попробуйте перезапустить сервис

### Проблема: Приложение не работает после деплоя

**Решение:**
1. Проверьте логи Railway (должны быть ошибки)
2. Проверьте консоль браузера (F12 → Console)
3. Проверьте Network tab (F12 → Network) - какие запросы падают

---

## 📋 Чеклист после git push:

- [ ] Код запушен на GitHub
- [ ] Подождал 2-3 минуты
- [ ] Проверил статус деплоя на Railway (должен быть "Deployed")
- [ ] Проверил логи (должны быть сообщения об успешной инициализации)
- [ ] Проверил приложение (работает ли создание расписания/записей)

**Если все пункты выполнены - всё работает!** ✅

---

## 💡 Полезные команды Railway CLI (опционально):

Если у вас установлен Railway CLI:

```bash
# Посмотреть логи
railway logs

# Перезапустить сервис
railway restart

# Проверить переменные окружения
railway variables
```

Но это не обязательно - всё можно сделать через веб-интерфейс Railway.

---

## ✅ Итог:

**Обычно после `git push` ничего делать не нужно!**

Railway автоматически:
- ✅ Задеплоит новый код
- ✅ Перезапустит сервер
- ✅ Инициализирует базу данных (если нужно)

**Нужно только:**
- ✅ Подождать 2-3 минуты
- ✅ Проверить логи
- ✅ Проверить, что приложение работает

**Если что-то не работает - нажмите "Redeploy" вручную на Railway.**

