# 🎯 Пошаговая инструкция - ШАГ ЗА ШАГОМ

## ✅ ШАГ 1: Что у вас есть после покупки хостинга?

После покупки VPS на Hoster.by вы должны были получить:

1. **IP-адрес сервера** (например: `185.123.45.67`)
2. **Логин** (обычно `root` или ваш логин)
3. **Пароль** (для входа на сервер)

**Где найти эту информацию?**
- В письме от Hoster.by
- В личном кабинете на сайте hoster.by
- В панели управления VPS

**Напишите мне:**
- Какой у вас IP-адрес?
- Какой логин?

---

## 🔌 ШАГ 2: Подключение к серверу с Windows

Вы на Windows, поэтому нужно подключиться к серверу через SSH.

### Вариант A: Через PowerShell (проще всего)

1. **Откройте PowerShell на Windows:**
   - Нажмите `Win + X`
   - Выберите "Windows PowerShell" или "Терминал"

2. **Введите команду подключения:**
   ```powershell
   ssh root@ваш-ip-адрес
   ```
   
   Например:
   ```powershell
   ssh root@185.123.45.67
   ```

3. **При первом подключении спросит:**
   ```
   The authenticity of host... can't be established.
   Are you sure you want to continue connecting (yes/no)?
   ```
   Напишите: `yes` и нажмите Enter

4. **Введите пароль:**
   - Введите пароль (при вводе пароль НЕ отображается - это нормально!)
   - Нажмите Enter

5. **Если все правильно, вы увидите:**
   ```
   root@server:~#
   ```
   или
   ```
   Welcome to Ubuntu...
   root@server:~#
   ```

**ПОЗДРАВЛЯЮ! Вы на сервере!** 🎉

---

### Вариант B: Через PuTTY (если PowerShell не работает)

1. **Скачайте PuTTY:**
   - Откройте: https://www.putty.org/
   - Скачайте `putty.exe`

2. **Запустите PuTTY**

3. **Введите данные:**
   - **Host Name (or IP address):** ваш IP-адрес (например: `185.123.45.67`)
   - **Port:** `22`
   - **Connection type:** выберите `SSH`
   - Нажмите кнопку **"Open"**

4. **Введите логин:**
   - Обычно `root`
   - Нажмите Enter

5. **Введите пароль:**
   - Введите пароль (не отображается - это нормально)
   - Нажмите Enter

6. **Готово! Вы на сервере!**

---

## ✅ ШАГ 3: Проверка - вы на сервере?

После подключения вы должны увидеть что-то вроде:

```
root@server:~#
```

или

```
dantistcil@server:~$
```

**Это значит вы на сервере Linux!**

Проверьте командой:
```bash
pwd
```

Должно показать что-то вроде: `/root` или `/home/dantistcil`

---

## 📦 ШАГ 4: Обновление системы (первая команда)

**Вы на сервере?** Тогда выполните:

```bash
apt update
```

Если попросит пароль или скажет "permission denied", используйте:

```bash
sudo apt update
```

**Что делает эта команда?**
- Обновляет список доступных программ
- Это безопасно, просто обновление информации

**Подождите, пока выполнится** (может занять 1-2 минуты)

---

## 📥 ШАГ 5: Установка Node.js

После того как `apt update` завершится, выполните:

```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
```

Подождите...

Затем:

```bash
apt-get install -y nodejs
```

Или если попросит права:

```bash
sudo apt-get install -y nodejs
```

**Это установит Node.js** (нужен для работы приложения)

**Проверьте установку:**

```bash
node --version
```

Должно показать: `v18.x.x` или `v20.x.x`

---

## 🔧 ШАГ 6: Установка PM2

PM2 - это программа, которая будет запускать ваше приложение автоматически.

```bash
npm install -g pm2
```

Или если нужны права:

```bash
sudo npm install -g pm2
```

---

## 📂 ШАГ 7: Загрузка вашего проекта на сервер

Теперь нужно загрузить файлы вашего проекта на сервер.

### Способ 1: Через Git (если код на GitHub)

```bash
# Установите Git
apt install git -y
# или
sudo apt install git -y

# Перейдите в папку /opt
cd /opt

# Скачайте проект (замените на ваш репозиторий)
git clone https://github.com/ваш-username/clinic-management.git clinic

# Перейдите в папку проекта
cd clinic
```

### Способ 2: Через SCP (с вашего Windows компьютера)

**На вашем Windows компьютере** (в PowerShell):

1. Откройте PowerShell
2. Перейдите в папку с проектом:
   ```powershell
   cd D:\clinic
   ```
3. Загрузите файлы на сервер:
   ```powershell
   scp -r * root@ваш-ip:/opt/clinic
   ```
   
   Например:
   ```powershell
   scp -r * root@185.123.45.67:/opt/clinic
   ```

4. Введите пароль когда попросит

**Затем на сервере:**

```bash
cd /opt/clinic
```

---

## 🔨 ШАГ 8: Установка зависимостей проекта

На сервере выполните:

```bash
cd /opt/clinic
npm run install-all
```

**Это займет несколько минут!** Подождите...

Затем:

```bash
npm run build
```

Это тоже займет время...

---

## ⚙️ ШАГ 9: Настройка переменных окружения

```bash
cd /opt/clinic/server
nano .env
```

Откроется редактор. Добавьте:

```
NODE_ENV=production
PORT=3001
```

**Как сохранить:**
- Нажмите `Ctrl + O` (сохранить)
- Нажмите `Enter` (подтвердить имя файла)
- Нажмите `Ctrl + X` (выйти)

---

## 🚀 ШАГ 10: Запуск приложения

```bash
cd /opt/clinic/server
pm2 start index.js --name clinic
pm2 save
pm2 startup
```

**Проверьте:**

```bash
pm2 status
```

Должно показать, что приложение запущено!

---

## 🌐 ШАГ 11: Откройте в браузере

Откройте в браузере на вашем компьютере:

```
http://ваш-ip-адрес:3001
```

Например:
```
http://185.123.45.67:3001
```

**Готово!** 🎉

---

## ❓ Что делать, если что-то не работает?

**Не могу подключиться:**
- Проверьте IP-адрес
- Проверьте пароль
- Убедитесь, что используете правильный логин (обычно `root`)

**Команда не работает:**
- Убедитесь, что вы на сервере (не на Windows)
- Попробуйте добавить `sudo` перед командой

**Не могу загрузить файлы:**
- Используйте Git (проще)
- Или загрузите через панель управления Hoster.by

---

## 📞 Нужна помощь?

Напишите мне:
1. На каком шаге вы сейчас?
2. Что показывает экран?
3. Какая ошибка (если есть)?

И я помогу! 😊

