# 💻 Запуск приложения локально на компьютере

## 🎯 Цель: Запустить приложение на вашем Windows компьютере

Это позволит увидеть, как работает приложение, пока настраиваем хостинг.

---

## ✅ ШАГ 1: Проверьте Node.js

Откройте PowerShell на Windows и выполните:

```powershell
node --version
npm --version
```

**Если показывает версию** (например, `v18.x.x`) - отлично! Переходите к шагу 2.

**Если ошибка** - нужно установить Node.js:
1. Скачайте с https://nodejs.org/ (версия LTS)
2. Установите
3. Перезагрузите компьютер
4. Проверьте снова: `node --version`

---

## 📦 ШАГ 2: Установите зависимости

В PowerShell перейдите в папку проекта:

```powershell
cd D:\clinic
```

Установите все зависимости:

```powershell
npm run install-all
```

Подождите... Это займет несколько минут.

---

## 🚀 ШАГ 3: Запустите приложение

### Вариант A: Запустить всё одной командой (РЕКОМЕНДУЕТСЯ)

```powershell
npm run dev
```

Эта команда запустит:
- Сервер (backend) на порту 3001
- Клиент (frontend) на порту 3000

**Подождите** - увидите сообщения о запуске.

---

### Вариант B: Запустить по отдельности

**Терминал 1 (Backend):**
```powershell
cd D:\clinic\server
npm run dev
```

**Терминал 2 (Frontend):**
```powershell
cd D:\clinic\client
npm start
```

---

## 🌐 ШАГ 4: Откройте в браузере

После запуска откройте браузер и перейдите:

```
http://localhost:3000
```

**Готово!** Вы должны увидеть интерфейс системы управления клиникой! 🎉

---

## 📋 Что вы увидите:

- Интерфейс с вкладками: "Записи", "Клиенты", "Услуги"
- Можно создавать клиентов
- Можно создавать записи
- Можно управлять услугами
- Автоматический расчет стоимости

---

## ⚠️ Важно:

- **Приложение работает только на вашем компьютере**
- **Данные хранятся локально** (в файле `server/clinic.db`)
- **Другие не смогут подключиться** (только вы)

---

## 🛑 Остановка приложения

Чтобы остановить:
- Нажмите `Ctrl + C` в терминале
- Или закройте окно терминала

---

## 🆘 Проблемы?

**Ошибка "порт занят":**
- Закройте другие программы, использующие порты 3000 или 3001
- Или измените порты в настройках

**Ошибка при установке:**
- Убедитесь, что Node.js установлен
- Попробуйте: `npm cache clean --force`
- Затем снова: `npm run install-all`

**Не открывается в браузере:**
- Убедитесь, что приложение запущено (видите сообщения в терминале)
- Попробуйте: `http://127.0.0.1:3000`

---

## ✅ После успешного запуска:

Когда увидите, что приложение работает локально, можно:
1. Продолжить настройку хостинга
2. Или использовать локально для тестирования

**Попробуйте запустить сейчас!** 😊

